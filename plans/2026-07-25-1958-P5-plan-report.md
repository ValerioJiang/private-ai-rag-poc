# Report di esecuzione — Piano P5 (asincronia e rifiniture)

Piano: [2026-07-25-1958-P5-plan.md](2026-07-25-1958-P5-plan.md).
Prompt dei sub-agenti: [2026-07-25-1958-P5-plan-subagent-prompts.md](2026-07-25-1958-P5-plan-subagent-prompts.md).

Esecuzione in modalità non presidiata (`/execute-plan-auto`), quattro fasi in
sequenza, un sub-agente e un commit per fase.

---

## Prerequisiti

Verificati dall'orchestratore il **25/07/2026 alle 20:38**, prima di lanciare la
fase 1. Tutti passati.

| Prerequisito | Esito | Uscita reale |
|---|---|---|
| PostgreSQL + pgvector | PASS | `archetype-lab-db-1` `Up 28 hours (healthy)`, `0.0.0.0:5434->5432/tcp` |
| Ollama | PASS | `GET http://localhost:11434/api/tags` → HTTP **200** |
| Modelli | PASS | `ollama list` mostra `bge-m3:latest` (1.2 GB) e `qwen2.5:7b-instruct` (4.7 GB) |
| PyPI raggiungibile | PASS | `pip index versions django-tasks` → `django-tasks (0.12.0)` |
| Interprete | PASS | `.venv\Scripts\python.exe --version` → **Python 3.12.10** |
| `DJANGO_SETTINGS_MODULE` | PASS | non impostato nell'ambiente (`manage.py` usa `config.settings.dev`) |
| `.env` | PASS | `Test-Path .env` → `True` |
| Versioni attese | PASS | Django **6.0.7**, djangorestframework 3.17.1, langchain-core 1.5.1, langchain-ollama 1.1.0, langchain-postgres 0.0.17, httpx 0.28.1 |
| `curl` | PASS | `curl 8.21.0 (Windows) libcurl/8.21.0` |
| `/health` | PASS | `200 {'status': 'ok', 'checks': {'database': ok, 'pgvector': 'vector 0.8.5', 'ollama': '7 modelli disponibili'}}` |
| Repository | PASS | branch `main`, HEAD `023367f`, working tree pulito salvo i due file `plans/` non tracciati |

**Stato del database all'avvio** — coincide con quello misurato in pianificazione
alle 19:58, nessuno scostamento da annotare:

| | Atteso dal piano | Misurato alle 20:38 |
|---|---|---|
| Documenti | 1 — id 7, `indexed`, 3 segmenti | 1 — `(7, 'indexed', 3)` |
| Basi di conoscenza | id 5 | id 5, «Base di conoscenza predefinita» |
| `QueryLog` | 58 | **58** |
| `RetrievedChunk` | 111 | **111** |
| File in `media/documents/` | 6 | **6** |
| `DBTaskResult` | tabella inesistente | tabella inesistente (le app della coda non sono ancora installate) |

---

## Fase 1 — Dipendenze, app e backend della coda (T-32)

Eseguita il **25/07/2026**, a partire da `023367f`. Sei passi (1.1 → 1.6), tutti
applicati come da piano. **Nessun comportamento cambiato**, come previsto:
nessun innesco dell'ingestione è stato toccato, quindi al termine della fase
l'ingestione è ancora sincrona.

### Passi applicati

| Passo | File | Esito |
|---|---|---|
| 1.1 | `requirements.in` | blocco «Coda dei task (P5, T-32)» inserito fra `# --- Framework ---` e `# --- Database e vector store ---`, con i commenti del piano |
| 1.2 | `requirements.txt` | rigenerato con `pip freeze` dopo installazione reale |
| 1.3 | `config/settings/base.py` | `django_tasks` e `django_tasks_db` in `INSTALLED_APPS`, dopo `rest_framework` e prima di `rag` |
| 1.4 | `config/settings/base.py` | blocco `TASKS` subito dopo `OLLAMA_BASE_URL` |
| 1.5 | `.env.example` | `TASKS_BACKEND=django_tasks_db.DatabaseBackend` dopo il blocco Ollama |
| 1.6 | — | `manage.py migrate` → 19 migrazioni `django_tasks_database` applicate |

### 1.2 — Le righe nuove di `requirements.txt`, una per una

`pip install -r requirements.in` ha installato **tre** pacchetti; tutti gli altri
erano già soddisfatti:

```
Installing collected packages: django-stubs-ext, django-tasks, django-tasks-db
Successfully installed django-stubs-ext-6.0.7 django-tasks-0.12.0 django-tasks-db-0.12.0
```

`git diff requirements.txt` → **tre** righe aggiunte, nessuna rimossa:

| Riga nuova | Provenienza | RNF-01 |
|---|---|---|
| `django-tasks==0.12.0` | diretta, `requirements.in` — il backport che porta l'API dei task | OK: nessun client di rete; il backend `db` parla solo col Postgres locale |
| `django-tasks-db==0.12.0` | diretta, `requirements.in` — il backend database e il comando `db_worker` | OK: usa l'ORM di Django sulla connessione `default`, cioè il Postgres in `docker-compose` sulla 5434 |
| `django-stubs-ext==6.0.7` | **transitiva** di `django-tasks` (dichiarata nei metadati del wheel) | OK: libreria di supporto ai type stub, patch di `__class_getitem__` sui generici; nessuna I/O, nessuna rete |

**Scostamento rispetto all'attesa del piano (minore, a favore).** Il piano
attendeva *quattro* righe nuove, includendo `typing_extensions`. Ne sono comparse
**tre**: `typing_extensions` era **già** in `requirements.txt` alla riga 43
(`typing_extensions==4.16.0`), tirata dentro da `pydantic`, e `pip` l'ha
riportata come «Requirement already satisfied». Nessun pacchetto inatteso: le tre
righe sono esattamente quelle previste meno una già presente. Nessuna verifica
RNF-01 aggiuntiva da fare.

**Scostamento di forma su `pip freeze`.** Il comando del piano
(`.venv\Scripts\python.exe -m pip freeze > requirements.txt`) eseguito da
PowerShell 5.1 scrive un **BOM UTF-8** in testa al file, che in questo ambiente è
il default della redirezione `>`. L'effetto sul diff era una riga di rumore
(`-annotated-types==0.8.0` / `+﻿annotated-types==0.8.0`) su un file che il
BOM non l'aveva mai avuto. Rigenerato con la stessa `pip freeze` da Git Bash, che
non lo aggiunge: il diff torna a **tre sole righe aggiunte, zero rimosse**. È una
proprietà della shell, non del comando; chi ripete il passo su PowerShell lo
tenga presente.

### Verifiche — uscite reali

**1. `manage.py check`** (misurato: **7,01 s**, primo avvio a freddo)

```
System check identified no issues (0 silenced).
exit=0
```

**2. `manage.py makemigrations --check --dry-run`**

```
No changes detected
exit=0
```

Nessuna migrazione dell'app `rag`, come richiesto dalla regola 6 dei prompt.

**3. `manage.py migrate`** — le 19 migrazioni di terze parti, in ordine:

```
Operations to perform:
  Apply all migrations: admin, auth, contenttypes, django_tasks_database, rag, sessions
Running migrations:
  Applying django_tasks_database.0001_initial... OK
  Applying django_tasks_database.0002_alter_dbtaskresult_options... OK
  Applying django_tasks_database.0003_dbtaskresult_enqueued_at_dbtaskresult_finished_at... OK
  Applying django_tasks_database.0004_dbtaskresult_started_at... OK
  Applying django_tasks_database.0005_alter_dbtaskresult_priority_and_more... OK
  Applying django_tasks_database.0006_alter_dbtaskresult_args_kwargs_and_more... OK
  Applying django_tasks_database.0007_add_separate_results_fields... OK
  Applying django_tasks_database.0008_separate_results_field... OK
  Applying django_tasks_database.0009_remove_results_field... OK
  Applying django_tasks_database.0010_alter_dbtaskresult_status... OK
  Applying django_tasks_database.0011_rename_complete_status... OK
  Applying django_tasks_database.0012_add_separate_exception_fields... OK
  Applying django_tasks_database.0013_separate_exception_fields... OK
  Applying django_tasks_database.0014_remove_dbtaskresult_exception_data... OK
  Applying django_tasks_database.0015_correctly_order_run_after... OK
  Applying django_tasks_database.0016_remove_dbtaskresult_django_task_new_ordering_idx_and_more... OK
  Applying django_tasks_database.0017_dbtaskresult_metadata... OK
  Applying django_tasks_database.0018_rename_succeeded_status... OK
  Applying django_tasks_database.0019_rename_django_task_new_ordering_idx_tasks_db_new_ordering_idx_and_more... OK
```

**4. `manage.py showmigrations django_tasks_database`** → **19** righe, tutte
`[X]`, dalla `0001_initial` alla
`0019_rename_django_task_new_ordering_idx_tasks_db_new_ordering_idx_and_more`.
Conferma il conteggio dichiarato da ARCHITECTURE §7.5 e il fatto che il `label`
dell'app è `django_tasks_database` e non il nome del modulo `django_tasks_db`.

**5. `manage.py db_worker --help`** — il comando esiste e porta tutte e sei le
opzioni che le fasi 2 → 4 useranno:

```
usage: manage.py db_worker [-h] [--queue-name [QUEUE_NAME]]
                           [--interval [INTERVAL]] [--batch]
                           [--reload | --no-reload] [--backend [BACKEND_NAME]]
                           [--no-startup-delay] [--max-tasks [MAX_TASKS]]
                           [--worker-id [WORKER_ID]] [--version]
                           ...
  --batch               Process all outstanding tasks, then exit. Can be used
                        in combination with --max-tasks.
  --reload, --no-reload
                        Reload the worker on code changes. Not recommended for
                        production as tasks may not be stopped cleanly
                        (default: DEBUG)
  --no-startup-delay    Don't add a small delay at startup.
```

Verificate presenti: `--batch`, `--interval`, `--max-tasks`, `--queue-name`,
`--no-reload`, `--no-startup-delay`. Il worker **non** è stato avviato: qui
bastava `--help`.

**6. `fase1.py`** (script fuori dal repository, eseguito con
`manage.py shell -c "exec(open(...).read())"`; misurato: **3,86 s**)

```
24 objects imported automatically (use -v 2 for details).

FASE 1 OK — coda configurata, 0 task, corpus intatto
exit=0
```

Le quattro sezioni passate: backend da `settings.TASKS` =
`django_tasks_db.DatabaseBackend`; `task_backends["default"]` è un'istanza di
`DatabaseBackend`; `DBTaskResult.objects.count() == 0`; corpus intatto (un
documento, `indexed`, 3 segmenti).

**7. `git diff --stat`** — tocca **solo** i quattro file della fase:

```
 .env.example            |  6 ++++++
 config/settings/base.py | 29 +++++++++++++++++++++++++++++
 requirements.in         | 10 ++++++++++
 requirements.txt        |  3 +++
 4 files changed, 48 insertions(+)
```

**8. Versioni installate**, verificate a runtime:

```
versioni: 0.12.0 0.12.0
DBTaskResult rows: 0
```

### `git grep -n "django\.tasks"` — il controllo **non** dà zero risultati, e non può darlo

La regola 3 dei prompt chiede «nessun risultato». Il comando letterale ne
restituisce **otto**, e **nessuno è un import**:

```
ARCHITECTURE.md:566:| django-q2 | ... Rimpiazzato in prospettiva da `django.tasks`; ...
ARCHITECTURE.md:570:Scelta: **API in stile `django.tasks` con backend database**. Il vantaggio
ARCHITECTURE.md:578:introduce `django.tasks` nella stdlib, ma ne spedisce **solo i backend
ARCHITECTURE.md:582:**backport** `django-tasks` (modulo `django_tasks`), non sopra `django.tasks`:
PLAN.md:14:| Async | ... **Nota:** `django.tasks` di Django 6 offre solo i backend ...
config/settings/base.py:136:# ATTENZIONE ALL'IMPORT: Django 6.0 ha una propria `django.tasks` che legge
config/settings/base.py:138:# codice si importa sempre `django_tasks` (underscore), mai `django.tasks`
requirements.in:10:# django.tasks di Django 6 esiste ma spedisce solo i backend `immediate` e
```

Cinque erano **già presenti** prima della fase (`ARCHITECTURE.md` ×4, `PLAN.md`
×1); le altre tre sono **prescritte dal piano stesso**, che ai passi 1.1 e 1.4 fa
scrivere commenti che nominano `django.tasks` proprio per metterne in guardia. La
verifica letterale non è quindi soddisfacibile da nessuna fase di P5, presente o
futura.

Sostituita con quella che misura ciò che la regola intende davvero — **nessun
import** di `django.tasks` in tutto il repository:

```
git grep -nE "(^|[^_a-zA-Z.])(from|import)[[:space:]]+django\.tasks"
→ nessun risultato (exit 1)
```

Suggerimento per le fasi 2 → 4: usare questa forma, non quella letterale.

### Scoperta: `django.tasks` **è** importato, ma da terze parti, e non fa danno

Controllo aggiuntivo (`"django.tasks" in sys.modules` dopo `django.setup()`) →
**True**. Tracciando gli import si trovano **due** importatori, entrambi di
terze parti e deliberati:

```
.venv/Lib/site-packages/django_stubs_ext/patch.py:112
    from django.tasks import Task, TaskContext, TaskResult
.venv/Lib/site-packages/django_tasks_db/compat.py:2
    from django.tasks.base import Task as DjangoTask
```

`django_tasks_db/compat.py` lo fa **di proposito**, in un `try/except ImportError`:

```python
TASK_CLASSES = (Task, DjangoTask) if DjangoTask is not None else (Task,)
```

cioè il backend DB accetta **sia** i `Task` del backport **sia** quelli della
stdlib. Il pacchetto `django-stubs-ext`, arrivato come transitiva al passo 1.2, è
il secondo importatore.

**Conseguenza sulla decisione 5 del piano.** Il piano afferma: «nulla in
`django/core/` importa `django.tasks`, quindi il check si registra **solo se lo
importa il codice applicativo**». La prima metà regge; la seconda **no** a
partire dalla 0.12.0, perché a importarlo sono le dipendenze. Misurato:

```
check di django.tasks registrato: ['django.tasks.checks.check_tasks']
check del backport:               ['django_tasks.checks.check_tasks']
istanza via django.tasks: django_tasks_db.backend.DatabaseBackend
esito del suo check(): []
```

`django/tasks/checks.py` istanzia il backend da `settings.TASKS` e ne chiama
`check()`. Poiché `TaskBackendHandler.create_connection()` si limita a
`import_string(backend)` e a costruirlo, la **gerarchia sbagliata non è un
problema**: si ottiene la stessa `django_tasks_db.backend.DatabaseBackend`, il
cui `check()` restituisce lista vuota. Entrambi i check girano, entrambi
passano, `manage.py check` resta a **0 issues**.

**Nessuna azione richiesta, nessun codice cambiato.** La regola operativa
(`django_tasks` con l'underscore nel nostro codice) resta valida e va comunque
rispettata: due handler distinti leggono lo stesso setting e costruiscono **due
istanze separate** del backend, e mescolarli non porterebbe alcun vantaggio. La
premessa della decisione 5 è però da correggere in `ARCHITECTURE.md` alla fase 4:
il check di `django.tasks` si registra **sempre**, e non è dannoso.

### Stato al termine della fase

| | Prima | Dopo |
|---|---|---|
| Documenti | 1 (id 7, `indexed`, 3 segmenti) | **invariato** |
| `DBTaskResult` | tabella inesistente | tabella creata, **0 righe** |
| Migrazioni di `rag` | nessuna pendente | **nessuna pendente** |
| `manage.py check` | 0 issues | 0 issues |
| Comportamento dell'ingestione | sincrona | **sincrona** (deliberato: nessun innesco toccato) |

**Commit:** `P5: coda dei task in Postgres — dipendenze, app e backend (T-32)`.
Committati i quattro file di configurazione più questa sezione di report; i due
file del piano in `plans/` restano non tracciati.

---

## Fase 2 — Ingestione asincrona (T-32)

Eseguita il **25/07/2026 alle 20:52**, a partire da `3aba5b3`. Sei passi
(2.1 → 2.6), applicati come un blocco unico: al termine **tutti e quattro** gli
inneschi passano da `accoda_indicizzazione()`, nessuno indicizza più in linea
salvo `manage.py ingest` senza `--async`, che resta sincrono per scelta
(decisione 12).

Prerequisiti ricontrollati prima di toccare qualunque cosa: `django_tasks` e
`django_tasks_db` **0.12.0** entrambi importabili, migrazioni
`django_tasks_database` fino alla `0019` tutte `[X]`, `DBTaskResult` a **0**
righe, corpus `[(7, 'indexed', 3)]`, working tree pulito su `3aba5b3`.

### Passi applicati

| Passo | File | Esito |
|---|---|---|
| 2.1 | `rag/tasks.py` (nuovo) | `indicizza_documento` (task) + `accoda_indicizzazione` (unico punto di accodamento), docstring copiate per intero |
| 2.2 | `rag/admin.py` | `save_model()` accoda; via `from .services.exceptions import IngestionError` e `ingest_document` dall'import successivo; dentro `from .tasks import accoda_indicizzazione`; nota di fase dell'intestazione riscritta |
| 2.3 | `rag/admin.py` | azione «Reindicizza» → N `enqueue`, nessuna ingestione in linea |
| 2.4 | `rag/views.py` | `documenti()` risponde **202**, ramo **422** rimosso, docstring e terza riga dell'intestazione riscritte, due import caduti |
| 2.5 | `rag/management/commands/ingest.py` | opzione `--async` (`dest="asincrono"`); i due `stdout.write` **sostituiti**, non duplicati |
| 2.6 | `rag/services/ingestion.py` | **sole docstring**: quella di `ingest_document()` e la nota del passo 3 di `_esegui_ingestione()` non parlano più di P5 al futuro. Nessuna riga di codice toccata |

### La misura che giustifica la fase — RNF-03

Il punto 1 dello script misura il tempo della `POST /api/documents/` dal lato del
client, con lo stesso `APIClient` usato in P4:

| | Misura | Rapporto |
|---|---|---|
| P4 — `POST` a freddo (`bge-m3` da caricare) | **14,53 s** | — |
| P4 — `POST` a caldo | **4,25 s** | — |
| **P5 — `POST` (accodamento)** | **0,141 s** (141 ms) | **103×** più veloce del caso a freddo, **30×** di quello a caldo |

Il tempo di accodamento è **sotto la soglia di 0,3 s** che il piano dichiarava
attesa (sezione 4, «Tempi da aspettarsi»), e la richiesta non tocca né Ollama né
pgvector: il lavoro non è sparito, è passato al worker, che nella stessa
esecuzione ha impiegato **12,78 s** a freddo per lo stesso documento (`bge-m3`
caricato in VRAM dal processo del worker, che è sempre a freddo alla prima
ingestione). Le esecuzioni successive del worker, a caldo, sono costate **3,0 s**
e **3,3 s** per documento.

L'azione «Reindicizza» dell'admin su un documento è tornata in **16 ms**
(punto 7): era il punto in cui P2 poteva durare N ingestioni.

### Le dieci sezioni di `fase2.py` — uscita reale

Script scritto nella directory scratch della sessione, **fuori dal repository**,
eseguito nella sola forma ammessa
(`manage.py shell -c "exec(open(...).read())"`, che è anche l'unica in cui
`Worker.configure_signals()` può chiamare `signal.signal()` dal thread
principale):

```
[stato iniziale] documenti=1 task=0 file media=6
[1] OK — 202 in 141 ms (0.141 s), documento 49 «pending», 0 segmenti
[2] OK — task 68a86566-bb51-49d4-83cb-ab8a38ff7f54 path=rag.tasks.indicizza_documento args=[49] status=READY
[3] OK — CONTROLLO NEGATIVO: dopo 3 s senza worker il documento e' ancora «pending»
[4] OK — worker (batch) in 12.78 s: documento «indexed», 1 segmenti, task SUCCESSFUL
[5] OK — DOPPIO REGISTRO DEL FALLIMENTO
[6] OK — deduplica intatta: 409, documenti invariati (3)
[7] OK — l'azione «Reindicizza» accoda e torna in 16 ms, documento «pending»
[8] OK — `ingest --async` accoda (documento 51 «pending», +1 task); `ingest` senza opzione indicizza in linea (documento 52 «indexed», +0 task)
[9] OK — coda svuotata: documenti dei punti 7 e 8 «indexed», 0 task in attesa
[10] OK — documenti di prova cancellati: documenti=1 (atteso 1); file media 6 → 10 (+4 orfani: la rimozione dal disco e' T-34, fase 3)

[coda] righe DBTaskResult per stato:
         READY: 0
       RUNNING: 0
        FAILED: 1
    SUCCESSFUL: 3
[corpus] [(7, 'indexed', 3)]

FASE 2 OK — accodamento, worker, fallimento persistito, deduplica intatta
```

**Il controllo negativo (punto 3) è passato**, ed è quello che rende non vuota
l'intera fase: dopo 3 s **senza alcun worker** il documento è ancora `pending`.
Un `enqueue` che eseguisse di nascosto in linea — il modo più facile di sbagliare
questa fase — avrebbe passato tutte le altre asserzioni e sarebbe fallito solo
qui.

### Punto 5, per esteso — il fallimento è registrato **due volte**

È la decisione 10 messa alla prova. Un PDF con una pagina e nessun testo, caricato
via API:

```
     POST         : 202
     documento    : status='failed'
     error_message: "Nessun testo estraibile dalle 1 pagine del documento. E' probabilmente
                     una scansione senza OCR: l'OCR e' dichiarato fuori ambito
                     (REQUIREMENTS §8), quindi questo file non e' indicizzabile."
     task         : status='FAILED'
     traceback (prima e ultima riga):
       Traceback (most recent call last):
       rag.services.exceptions.PdfSenzaTesto: Nessun testo estraibile dalle 1 pagine del
       documento. E' probabilmente una scansione senza OCR: l'OCR e' dichiarato fuori
       ambito (REQUIREMENTS §8), quindi questo file non e' indicizzabile.
```

I due registri hanno due destinatari e due contenuti diversi, come la decisione
10 prevedeva:

- **sul documento**, per l'amministratore: `status='failed'` più il motivo in
  italiano, leggibile nell'admin e su `GET /api/documents/{id}/`. È RNF-04, ed è
  ciò che sostituisce il 422 sparito dalla POST;
- **sulla riga `DBTaskResult`**, per chi sviluppa: `status='FAILED'` più il
  traceback completo, che parte da `db_worker.run_task` e arriva a
  `loaders.py:83`. La coda **non** è verde sopra a un documento fallito.

Uscita reale del worker sul fallimento, dal log della stessa esecuzione:

```
WARNING rag.services.ingestion Ingestione del documento 50 rifiutata: Nessun testo estraibile …
ERROR   django_tasks Task id=013be9eb-4df9-4c23-8872-3c67eb85e9a1 path=rag.tasks.indicizza_documento state=FAILED
Traceback (most recent call last):
  File "…\django_tasks_db\management\commands\db_worker.py", line 168, in run_task
  File "…\django_tasks\base.py", line 165, in call
  File "…\rag\tasks.py", line 72, in indicizza_documento
  File "…\rag\services\ingestion.py", line 154, in ingest_document
  File "…\rag\services\ingestion.py", line 225, in _esegui_ingestione
  File "…\rag\services\loaders.py", line 83, in load_pdf
rag.services.exceptions.PdfSenzaTesto: …
```

### Punto 8 — le due asserzioni simmetriche sul comando

`ingest --async` ha accodato (documento 51 `pending`, **+1** riga di task) e
`ingest` senza opzione ha indicizzato in linea (documento 52 `indexed`, **+0**
righe di task), stampando l'avvertimento riscritto al passo 2.5:

```
Documento 51 accodato (task 10a8f305-85be-4e6a-8960-c16ea0de0186). Lo esegue il worker: manage.py db_worker
Indicizzazione del documento 52 nella base «Base di conoscenza predefinita»…
Questo comando indicizza in linea e attende: il primo embedding puo' richiedere fino a
~20 s per il caricamento del modello in VRAM. Per accodare e tornare subito: --async.
Fatto — documento 52: 1 segmenti su 1 pagine in 3.0s
```

La seconda asserzione (**+0** task) è quella che protegge la decisione 12: se il
comando diventasse asincrono per difetto, **T-42 e T-43 smetterebbero di
funzionare senza worker**.

### Verifiche di chiusura — uscite reali

```
.venv\Scripts\python.exe manage.py check
System check identified no issues (0 silenced).

.venv\Scripts\python.exe manage.py makemigrations --check --dry-run
No changes detected

git status --short
 M rag/admin.py
 M rag/management/commands/ingest.py
 M rag/services/ingestion.py
 M rag/views.py
?? plans/2026-07-25-1958-P5-plan-subagent-prompts.md
?? plans/2026-07-25-1958-P5-plan.md
?? rag/tasks.py
```

**Import morti** — controllati a vista e con `grep` su tutta l'app: in `admin.py`
`ingest_document` sopravvive **solo** alla riga 317, dentro la docstring di
`DocumentAdminForm` (non è un uso); in `views.py` **solo** alla riga 4,
nell'intestazione riscritta al passo 2.4. `IngestionError` non compare più in
nessuno dei due. Restano legittimamente in `rag/management/commands/ingest.py`
(righe 15–16, 96–97), che è l'unica via sincrona rimasta.

**Nessun import di `django.tasks`:**

```
git grep -nE "(^|[^_a-zA-Z.])(from|import)[[:space:]]+django\.tasks" -- '*.py'
→ nessun risultato (exit 1)
```

### Scostamenti

**1. Lo stato del task riuscito si chiama `SUCCESSFUL`, non `SUCCEEDED`.** Lo
script del piano asserisce `riga.status == "SUCCEEDED"`; il valore reale, letto
da `TaskResultStatus` della 0.12.0, è **`SUCCESSFUL`** — i quattro membri sono
`READY`, `RUNNING`, `FAILED`, `SUCCESSFUL`. È esattamente il caso previsto dalla
nota del piano («se l'asserzione fallisce sul **valore**, correggere lo script»):
lo script è stato corretto **prima** di eseguirlo, sostituendo le stringhe con i
simboli `TaskResultStatus.SUCCESSFUL` / `TaskResultStatus.FAILED`. Il codice non
è stato toccato. Le migrazioni `0011_rename_complete_status` e
`0018_rename_succeeded_status` sono la traccia storica di questa rinomina.

**2. Aggiunta oltre il piano: la descrizione del fieldset di `DocumentAdmin`.**
Il passo 2.2 prescrive di aggiornare la nota di fase nell'intestazione del
modulo, ma non la `description` del primo fieldset di `DocumentAdmin`, che
diceva: «Il salvataggio avvia immediatamente l'indicizzazione, che e' SINCRONA:
la pagina resta in attesa per tutta la sua durata». È un testo **visibile
all'utente nell'admin** e da questa fase è falso — chi valuta il progetto lo
legge esattamente nel punto in cui carica un PDF. Riscritto per dire che il
documento va in coda, che la pagina non attende e che senza worker resta «In
attesa» a tempo indeterminato. È un'aggiunta, non uno scostamento dal codice
prescritto: nessuna riga del piano è stata cambiata.

**3. `git grep` letterale su `django\.tasks`, forma non soddisfacibile.**
Confermato quanto misurato dalla fase 1. La forma che misura l'intento vero
(nessun **import**) restituisce **due** risultati sull'intero repository, ed
entrambi sono nel file di report della fase 1 alle righe 244 e 246, dove
**citano il sorgente di terze parti** (`django_stubs_ext/patch.py` e
`django_tasks_db/compat.py`). Ristretta ai file `.py` — cioè al codice — la
verifica dà **zero risultati**.

**4. `rag/serializers.py` porta ancora una riga da correggere, fuori perimetro.**
L'intestazione del modulo (riga 4) dice che le viste sono «involucri HTTP attorno
a `ingest_document()` (P2) e `rispondi()` (P3)»: la prima metà non è più vera. La
regola 7 dei prompt vieta di toccare `rag/serializers.py`, quindi **non è stata
modificata**. Va corretta alla fase 4, insieme alla documentazione.

**5. `media/documents/` resta con 4 file orfani, ed è atteso.** I quattro
documenti di prova (49, 50, 51, 52) sono stati cancellati con i loro vettori — il
receiver di `signals.py` li ha rimossi, e lo si legge nel log («Rimossi 1 vettori
del documento 49 …») — ma il file su disco resta, perché `FileField` non lo
cancella. È il debito che **T-34 chiude alla fase 3** con il receiver
`rimuovi_file`: conteggio **6 → 10**. Non sistemato qui di proposito.

### Stato al termine della fase

| | Prima | Dopo |
|---|---|---|
| Documenti | 1 (id 7, `indexed`, 3 segmenti) | **1 (id 7, `indexed`, 3 segmenti)** — corpus riportato allo stato iniziale |
| `DBTaskResult` | 0 righe | **4 righe**: 3 `SUCCESSFUL`, 1 `FAILED`, 0 `READY`, 0 `RUNNING` — sono il registro della coda e restano |
| File in `media/documents/` | 6 | **10** (+4 orfani, cfr. scostamento 5) |
| Migrazioni di `rag` | nessuna pendente | **nessuna pendente** |
| `manage.py check` | 0 issues | 0 issues |
| Comportamento dell'ingestione | sincrona | **asincrona** su tutti gli inneschi HTTP e dell'admin; sincrona solo da `manage.py ingest` senza `--async` |
| Contratto di `POST /api/documents/` | 201 / 400 / 409 / 422 | **202 / 400 / 409** — il 422 non esiste più (decisione 11) |

**Commit:** `P5: ingestione asincrona su coda Postgres (T-32)`. Committati i
cinque file di codice più questa sezione di report; i due file del piano in
`plans/` restano non tracciati.

---

## Fase 3 — Errori uniformi, residui e osservabilità (T-34)

Eseguita il **25/07/2026 alle 21:00**, a partire da `527182b`. Sei passi
(3.1 → 3.6), applicati come da piano. Chiude i **tre debiti accertati** che il
report di P4 aveva lasciato aperti (404 in inglese, file orfani in `MEDIA_ROOT`,
pagina HTML sui guasti inattesi) più i **due** che l'asincronia rende visibili
(due processi indistinguibili nei log, nessun modo di sapere se un documento in
attesa aspetta un worker occupato o un worker mai avviato).

Stato ricontrollato prima di toccare qualunque cosa: HEAD `527182b`, working tree
pulito salvo i due file `plans/` non tracciati; corpus `[(7, 'indexed', 3)]`;
`DBTaskResult` **4 righe** (3 `SUCCESSFUL`, 1 `FAILED`, 0 `READY`, 0 `RUNNING`);
`media/documents/` **10 file**.

### Passi applicati

| Passo | File | Esito |
|---|---|---|
| 3.1 | `rag/errors.py` (nuovo) | `gestore_eccezioni()`, docstring copiata per intero |
| 3.2 | `rag/views.py` | `documento()` usa `.filter(pk).first()` + `NotFound(f"Nessun documento con id {pk}.")`; `get_object_or_404` esce dagli import, entra `NotFound` |
| 3.3 | `rag/views.py` | `_check_coda()`, quarto controllo di `/health`, che **non fa mai fallire** l'health check |
| 3.4 | `config/settings/base.py` | `REST_FRAMEWORK["EXCEPTION_HANDLER"] = "rag.errors.gestore_eccezioni"` |
| 3.5 | `config/settings/base.py` | `LOGGING`: `[pid {process}]` nel formato, logger `django_tasks` e `django_tasks_db` |
| 3.6 | `rag/signals.py` | receiver `rimuovi_file` dopo `rimuovi_vettori`, stessa disciplina (`post_delete` → `on_commit`, errori loggati e non risollevati); intestazione del modulo estesa |

### Linea di base di `MEDIA_ROOT`: **10 file**, non 6 — e delta zero

Il piano asserisce «alla fine il conteggio deve essere quello iniziale (**6**)».
Alla partenza di questa fase i file erano **10**: la fase 2 ha caricato quattro
PDF di prova e poi ha cancellato i documenti, e i file sono rimasti orfani sul
disco — cioè **esattamente il debito che questa fase chiude, misurato dal vivo**
(scostamento 5 della fase 2). Quei quattro orfani il receiver `rimuovi_file`
**non** li rimuove, perché le righe `Document` sono già sparite: la pulizia è
compito della fase 4.

L'asserzione è stata quindi riscritta come **delta zero rispetto alla linea di
base misurata all'avvio**, non come valore assoluto — che è ciò che la sezione 3
del piano prescrive («le asserzioni delle verifiche contano *delta*, non valori
assoluti»). Misura reale:

```
[stato iniziale] documenti=1 file media=10
[4] file p5-file.pdf creato (10 → 11) e rimosso alla cancellazione (10)
[chiusura] file media 10 → 10 (delta 0)
```

### Le sei sezioni di `fase3.py` — uscita reale

Script nella directory scratch della sessione, **fuori dal repository**, eseguito
nella sola forma ammessa
(`manage.py shell -c "exec(open(..., encoding='utf-8').read())"`, l'unica in cui
`Worker.configure_signals()` può chiamare `signal.signal()` dal thread
principale):

```
[stato iniziale] documenti=1 file media=10
[1] OK — 404 italiano: ErrorDetail(string='Nessun documento con id 999999.', code='not_found')
[2] OK — 500 application/json: "Errore interno del server. Il motivo e' registrato nel log dell'applicazione."; «guasto simulato» non trapela
[3] OK — domanda vuota 400 {'detail': "La domanda e' vuota: non c'e' nulla da cercare nei documenti."}; ?status=inventato 400 {'status': ErrorDetail(string='Stato «inventato» inesistente. Ammessi: pending, processing, indexed, failed.', code='invalid')}
[4] OK — documento caricato (202), file p5-file.pdf creato (10 → 11) e rimosso alla cancellazione (10)
[4b] OK — ramo «documento sparito»: task eb395b8b-eac9-48f0-8536-91fd6ae3d8ea SUCCESSFUL, return_value={'esito': 'documento inesistente', 'document_id': 53}; ready=0
[5] OK — coda: coda su database: 0 in attesa, 0 in esecuzione (le lavora `manage.py db_worker`)
[6] OK — formato: {levelname} {asctime} [pid {process}] {name} {message}
[chiusura] file media 10 → 10 (delta 0)
[coda] {'READY': 0, 'RUNNING': 0, 'FAILED': 1, 'SUCCESSFUL': 4}
[corpus] [(7, 'indexed', 3)]
FASE 3 OK — 404 italiano, 500 JSON, file rimosso, coda in /health
```

**Punto 1 — il 404 parla italiano e dice *quale* id.** L'asserzione **negativa**
è passata: `"No Document matches"` non compare più in alcuna risposta. Il
messaggio è quello della vista, non il generico del gestore: la rete di sicurezza
di `rag/errors.py` traduce `Http404` in «Risorsa non trovata.», ma `documento()`
non ci arriva mai perché solleva `NotFound` per conto proprio — che è la
distinzione della decisione 15 («il gestore è la rete di sicurezza, non la
cura»).

**Punto 2 — il guasto inatteso è un 500 JSON e non trapela.** Entrambi i
controlli passati: `Content-Type: application/json` con `DEBUG=True` (scelta
dichiarata, decisione 15 — sulle rotte `/api/` non c'è più la pagina di debug), e
il testo interno «guasto simulato» **non** compare nella risposta. Lo stack resta
sulla console per intero, via `logger.exception`, con il pid nuovo:

```
ERROR 2026-07-25 20:59:59,714 [pid 7432] rag.errors Guasto inatteso su POST /api/ask/: guasto simulato
Traceback (most recent call last):
  File "…\rest_framework\views.py", line 512, in dispatch
  File "…\rest_framework\decorators.py", line 50, in handler
  File "…\rag\views.py", line 321, in ask
  File "<string>", line 43, in esplode
RuntimeError: guasto simulato
```

Il logger registra **metodo e percorso** (`POST /api/ask/`) e non `context["view"]`:
le viste sono funzioni decorate con `@api_view` e DRF le avvolge tutte nella
stessa classe generata, che avrebbe detto «WrappedAPIView» per ognuna.

**Punto 3 — le condizioni previste non sono regredite.** È il controllo che un
gestore scritto male fallirebbe da solo mentre tutto il resto passerebbe: la
domanda vuota resta **400** con il messaggio del dominio
(«La domanda e' vuota: non c'e' nulla da cercare nei documenti.») e
`?status=inventato` resta **400** con l'elenco degli stati ammessi. Nessuna delle
due è diventata un 500.

**Punto 4 — il file sparisce col documento, e il task orfano finisce bene.** Il
PDF caricato via API (202) esiste su disco, e alla `documento.delete()` sparisce
da sé:

```
INFO 2026-07-25 20:59:59,972 [pid 7432] rag.signals File «documents/2026/07/p5-file.pdf» del documento 53 rimosso da MEDIA_ROOT.
```

Il documento è stato cancellato con il **task ancora in coda**: il worker in
`--batch` ha eseguito il ramo «documento sparito» di `indicizza_documento()`, che
si conclude con **successo** — cancellare un documento è legittimo, non un
guasto — e lascia la coda senza pendenti:

```
INFO … django_tasks Task id=eb395b8b… path=rag.tasks.indicizza_documento state=RUNNING
WARNING … rag.tasks Documento 53 cancellato prima dell'indicizzazione: task senza effetto.
INFO … django_tasks Task id=eb395b8b… path=rag.tasks.indicizza_documento state=SUCCESSFUL
INFO … django_tasks_db No more tasks to run for worker_id=olBsayHi0zUNadSZGxdTBTx4Pl3j8Vpb - exiting gracefully.
```

`DBTaskResult.objects.ready().count() == 0`: **la fase 4 non eredita alcun task
pendente.**

**Punti 5 e 6 — `/health` e i log.** `/health` resta **200 `ok`** con la voce
«coda» presente e `ok: true`; il formato dei log porta il pid, e i due logger
della coda sono configurati.

### Il dettaglio che `/health` riporta sotto «coda» — alla lettera

A coda vuota (misurato da `fase3.py`, punto 5):

```
coda su database: 0 in attesa, 0 in esecuzione (le lavora `manage.py db_worker`)
```

Con un documento **davvero in attesa** — cioè il caso per cui il controllo esiste
— misurato con uno script aggiuntivo (`fase3b.py`) che carica un PDF, legge
`/health` mentre il documento è `pending`, poi cancella e svuota la coda:

```
coda (con 1 documento in attesa): coda su database: 1 in attesa, 0 in esecuzione (le lavora `manage.py db_worker`)
coda (a coda vuota):              coda su database: 0 in attesa, 0 in esecuzione (le lavora `manage.py db_worker`)
[chiusura] file media 10 → 10; ready=0; corpus=[(7, 'indexed')]
```

In entrambi i casi `"status": "ok"` e `checks.coda.ok = true`: **una coda con
task in attesa non fa mai fallire l'health check**, come prescrive la docstring
del passo 3.3. È la riga che dice a chi valuta il progetto che il documento non è
bloccato ma solo in attesa, e chi deve lavorarlo — `manage.py db_worker`.

**Anche il ramo alternativo è stato misurato** (non richiesto dal piano, ma è
l'altro percorso della funzione appena scritta): con
`TASKS_BACKEND=django_tasks.backends.immediate.ImmediateBackend`, cioè il ripiego
della decisione 4, `_check_coda()` restituisce

```
(True, 'ImmediateBackend: esecuzione in linea, nessun worker necessario')
```

senza toccare `DBTaskResult`. Il backend è letto da `settings.TASKS` e non da
`default_task_backend`, che è un `ConnectionProxy`.

### Verifiche di chiusura — uscite reali

```
.venv\Scripts\python.exe manage.py check
System check identified no issues (0 silenced).

.venv\Scripts\python.exe manage.py makemigrations --check --dry-run
No changes detected

git status --short
 M config/settings/base.py
 M rag/signals.py
 M rag/views.py
?? plans/2026-07-25-1958-P5-plan-subagent-prompts.md
?? plans/2026-07-25-1958-P5-plan.md
?? rag/errors.py

git diff --stat
 config/settings/base.py | 18 ++++++++++++++-
 rag/signals.py          | 60 ++++++++++++++++++++++++++++++++++++++++++++++---
 rag/views.py            | 55 ++++++++++++++++++++++++++++++++++++++-------
 3 files changed, 121 insertions(+), 12 deletions(-)
```

**Nessun import di `django.tasks`** (forma che misura l'intento vero, cfr. gli
scostamenti delle fasi 1 e 2):

```
git grep -nE "(^|[^_a-zA-Z.])(from|import)[[:space:]]+django\.tasks" -- "*.py"
→ nessun risultato (exit 1)
```

**Nessun `get_object_or_404` residuo**: `git grep -n "get_object_or_404" -- "*.py"`
dà **una** sola riga, `rag/views.py:270`, che è **dentro la docstring** di
`documento()` e spiega perché non si usa più. L'import è caduto.

### Scostamenti

**1. La linea di base di `MEDIA_ROOT` è 10, non 6** — trattata sopra. L'asserzione
del piano («conteggio finale 6») è stata riscritta come **delta zero rispetto ai
10 misurati all'avvio**; i quattro orfani della fase 2 **non** sono stati
cancellati a mano, perché la pulizia è assegnata alla fase 4 e cancellarli qui
avrebbe nascosto la misura del debito.

**2. `ultimo.status == "SUCCEEDED"` non esiste.** Lo script del piano confronta
con la stringa `"SUCCEEDED"`; il valore reale della 0.12.0 è **`SUCCESSFUL`**, già
misurato dalla fase 2 (scostamento 1). Lo script è stato corretto **prima** di
eseguirlo, confrontando col simbolo `TaskResultStatus.SUCCESSFUL` invece che con
la stringa. Nessuna riga di codice toccata.

**3. `git grep -n "django\.tasks"` letterale resta non soddisfacibile**, come
misurato dalle fasi 1 e 2: il piano stesso fa scrivere commenti che *nominano*
`django.tasks` per metterne in guardia. Usata la forma che misura l'assenza di
**import**, riportata sopra.

**4. Aggiunta oltre il piano, nell'intestazione di `rag/signals.py`.** Il passo
3.6 chiede di estendere l'intestazione «di un paragrafo». Oltre al paragrafo
nuovo («IL SECONDO RESIDUO E' IL FILE»), sono state toccate due righe di
contesto: il titolo del modulo, che diceva «Rimozione dei **vettori** alla
cancellazione di un documento (RF-08, T-20)» ed è diventato «Rimozione dei
**residui** … (RF-08, T-20, T-34)», e l'intestazione del limite dichiarato, ora
«LIMITE RESIDUO **SUI VETTORI**». Senza queste due, il modulo avrebbe dichiarato
di occuparsi solo dei vettori e il limite si sarebbe letto come valido anche per
i file, che non è vero — il limite sui file è un altro, ed è dichiarato nella
docstring di `rimuovi_file` (sostituire il file di un documento esistente lascia
il precedente su disco).

**5. `rag/serializers.py` riga 4 resta da correggere, fuori perimetro.** Confermato
quanto annotato dalla fase 2 (scostamento 4): l'intestazione dice ancora che le
viste sono involucri attorno a `ingest_document()`. La regola 7 vieta di toccare
quel file; la correzione è della fase 4.

### Stato al termine della fase

| | Prima | Dopo |
|---|---|---|
| Documenti | 1 (id 7, `indexed`, 3 segmenti) | **1 (id 7, `indexed`, 3 segmenti)** — invariato |
| File in `media/documents/` | 10 | **10** — delta zero; i 4 orfani della fase 2 restano, li pulisce la fase 4 |
| `DBTaskResult` | 4 righe (3 `SUCCESSFUL`, 1 `FAILED`) | **6 righe**: 5 `SUCCESSFUL`, 1 `FAILED`, **0 `READY`**, 0 `RUNNING` — le due nuove sono i task dei documenti cancellati (ramo «documento sparito»), e restano come registro |
| 404 su id inesistente | `{"detail": "No Document matches the given query."}` | **`{"detail": "Nessun documento con id 999999."}`** |
| Guasto inatteso su `/api/` | pagina HTML | **500 JSON**, stack nel log |
| Cancellazione di un documento | vettori rimossi, **file orfano** | vettori **e** file rimossi |
| `/health` | 3 controlli | **4 controlli**, con «coda» che non fa mai fallire la sonda |
| Formato dei log | `{levelname} {asctime} {name} {message}` | `{levelname} {asctime} **[pid {process}]** {name} {message}` + logger `django_tasks`, `django_tasks_db` |
| Migrazioni di `rag` | nessuna pendente | **nessuna pendente** |
| `manage.py check` | 0 issues | 0 issues |

**Commit:** `P5: errori uniformi, 404 in italiano, file rimossi con il documento (T-34)`.
Committati i quattro file di codice (`rag/errors.py` nuovo,
`config/settings/base.py`, `rag/views.py`, `rag/signals.py`) più questa sezione di
report; i due file del piano in `plans/` restano non tracciati.

---
