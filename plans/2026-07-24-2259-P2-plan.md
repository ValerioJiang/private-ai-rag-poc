# Piano P2 — Ingestione dei documenti

**Progetto:** sistema RAG in Django con LangChain e LLM privato locale (prova tecnica).
**Fase:** P2, attività T-14 → T-20 di [BACKLOG.md](../BACKLOG.md).
**Stima:** 6 h 30 su cinque fasi (backlog: 1h + 45m + 1h + 1h30 + 30m + 1h + 45m).
**Scadenza complessiva del progetto:** lunedì 27 luglio 2026, ore 9:30.
**Fase precedente:** [P1](2026-07-24-1834-P1-plan.md), chiusa il 24/07/2026 — [report](2026-07-24-1834-P1-plan-report.md).

---

## Problema / Obiettivo

Al termine di P1 il sistema è **configurabile ma vuoto**: undici modelli, un admin
navigabile, una configurazione predefinita funzionante, e nessun modo di mettere
un documento dentro la base di conoscenza. `Document` esiste, `DocumentChunk`
esiste, la macchina a stati è scritta nel modello — ma nulla la fa girare, e un
documento caricato dall'admin resta in stato «In attesa» per sempre.

P2 costruisce il **percorso che porta un PDF a diventare vettori interrogabili**:
estrazione del testo pagina per pagina, segmentazione secondo il
`ChunkingProfile` della base di conoscenza, calcolo degli embedding, scrittura in
pgvector e riconciliazione con le righe `DocumentChunk`. È anche la fase in cui
nasce il **factory layer** (`rag/services/factories.py`), la cerniera che
ARCHITECTURE §3 pone al centro dell'architettura: l'unico punto del progetto che
traduce righe di configurazione in oggetti LangChain.

Al termine di P2 **non si risponde ancora a domande**: la catena di generazione è
P3. Quello che deve esistere è un indice popolato e ispezionabile dall'admin.

Requisiti coperti: **RF-01 → RF-10** e **RF-29**.
Criteri di accettazione abilitati: **CA-2**, **CA-8**.

**Verifica di fase (da BACKLOG.md):** upload dall'admin → stato *indicizzato*,
chunk visibili con pagina e ordinale.

---

## Contesto

### Documenti da leggere prima di iniziare

Sono già nel repository e contengono le decisioni prese. Il piano li cita per
paragrafo, ma l'esecutore deve averli letti almeno una volta:

- `ARCHITECTURE.md` **§6.3** (le due metà dello schema: `langchain_pg_*` non sono
  modelli Django) e **§6.5** (duplicazione consapevole del testo dei chunk) — sono
  la specifica del problema centrale di questa fase
- `ARCHITECTURE.md` §7.4 (strategie di chunking), **§7.9** (perché `PGVector` e non
  un retriever custom; due ORM e due driver come prezzo dichiarato), **§7.10**
  (perché PyMuPDF diretto e non `PyMuPDFLoader`; il codice di estrazione dello
  spike **va promosso, non buttato**), §8.1 (la dimensione del vettore è un
  vincolo applicativo, non di schema), §8.5 (PDF scansionati non supportati)
- `REQUIREMENTS.md` §3.1 (RF-01 → RF-10), §6 UC-1 con le sue estensioni, §7 (CA-2, CA-8)
- `BACKLOG.md`, sezione P2 — attività T-14 → T-20
- `plans/2026-07-24-1834-P1-plan-report.md`, sezione **«Debiti aperti verso P2»** —
  sei voci accertate in P1 da non riscoprire. Sono tutte indirizzate da questo piano
- `scripts/spike_rag.py`, righe 57-95 — `load_pdf()` e la chiamata a
  `PGVector.from_documents()` che questa fase struttura

### Dati di realtà misurati durante questa sessione di pianificazione

Non sono ipotesi: sono stati verificati sul codice installato e sul database in
esecuzione il 24/07/2026. Risparmiano all'esecutore le scoperte costose.

| Dato | Valore verificato | Conseguenza per P2 |
|---|---|---|
| `langchain-postgres` installata | **0.0.17** | `PGVector` con la firma documentata sotto |
| `PGVector.add_documents(docs, ids=[...])` | accetta id **stringa arbitraria** e li restituisce | si usano id **deterministici**, cfr. decisione 1 |
| Scrittura con id già presente | **UPSERT**: `ON CONFLICT (id) DO UPDATE` di `embedding`, `document`, `cmetadata` | la reindicizzazione è idempotente, non duplica |
| `PGVector.add_texts()` | chiama `embed_documents(tutti i testi)` in **una sola** richiesta HTTP | `batch_size` va rispettato dal servizio, cfr. decisione 4 |
| `PGVector.delete(ids=[...])` | cancella per la colonna id personalizzata; nessun embedding calcolato | la cancellazione dei vettori **non richiede Ollama** |
| `PGVector.__init__` → `__post_init__` | esegue **DDL a ogni costruzione**: `CREATE EXTENSION`, `create_tables_if_not_exists()`, `create_collection()` | costruire lo store una volta per ingestione, e passare `create_extension=False` |
| `langchain_pg_embedding.id` | `character varying` (PK) | `DocumentChunk.vector_id` è un `CharField`: già corretto in P1 |
| `OllamaEmbeddings` — campi disponibili | `model`, `base_url`, `dimensions`, `num_ctx`, `keep_alive`, … **nessun `batch_size`, nessun `normalize`** | cfr. decisioni 4 e 5 |
| Norma L2 dei vettori di `bge-m3` via Ollama | **1.000000** su due campioni | `EmbeddingProfile.normalize` è già soddisfatto dal modello: decisione 5 |
| Dimensione confermata | **1024** | `EmbeddingProfile.dimension` è verificabile a runtime: decisione 6 |
| `TokenTextSplitter(chunk_size=…)` | `ImportError: Could not import tiktoken` | `tiktoken` **non** è installato: decisione 3 |
| `RecursiveCharacterTextSplitter.__init__` | `separators`, `keep_separator`, `is_separator_regex`, poi `**kwargs` → `chunk_size`, `chunk_overlap`, `add_start_index`, `length_function` | firma da rispettare nella factory |
| PDF corrotto | `pymupdf.FileDataError` (sottoclasse di `RuntimeError`) | CA-8: da catturare |
| PDF vuoto (0 byte) | `pymupdf.EmptyFileError`, **sottoclasse di `FileDataError`** | catturare `FileDataError` copre entrambi |
| **File inesistente** | `pymupdf.FileNotFoundError`, che **ombreggia l'omonima builtin e NON la sottoclassa**: deriva da `RuntimeError` | trappola: `except FileNotFoundError` non lo catturerebbe. Cfr. passo 1.3 |
| POST all'admin di `Document` senza i campi del management form dell'inline | **200 con errore «ManagementForm … Campi mancanti: chunks-TOTAL_FORMS, chunks-INITIAL_FORMS»**, e nessuna riga creata | ogni POST di verifica deve includere i quattro campi `chunks-*`, altrimenti «nessun documento creato» sembra un successo della deduplica |
| Prefisso del formset inline dei segmenti | **`chunks`** (dal `related_name`, non `documentchunk_set`) | nome dei campi del management form |
| `FieldFile.closed` di un documento appena letto dal database | `True`; diventa `False` dopo `seek()`; `close()` lo richiude | permette a `compute_checksum` di ripristinare lo stato di partenza |
| Su Windows, cancellare il file mentre **un'altra** istanza di `FieldFile` sullo stesso percorso è aperta | `PermissionError [WinError 32]` (osservato) | `compute_checksum` non deve lasciare handle aperti; cfr. passo 3.2 |
| `isinstance(x, UploadedFile)` | `True` per un file appena caricato, **`False` per un `FieldFile`** già salvato | è il discriminante corretto nel `clean()` del form (passo 4.4) |
| Nomi delle tabelle Django | `rag_document`, `rag_documentchunk`, … (nessun `db_table` esplicito) | per le eventuali verifiche in SQL grezzo |
| PyMuPDF installato | **1.28.0**; nome canonico del modulo `pymupdf`, `fitz` è alias | nel codice nuovo usare `import pymupdf` |
| `samples/manuale-dipendenti.pdf` | 3 pagine, tutte con testo → **3 chunk** a 800/120 | il PDF di prova è piccolo: non stressa il batching |
| Costo embedding a caldo | 3 chunk in **2,45 s** (≈0,8 s/chunk, richiesta singola) | l'ingestione sincrona di un PDF grande è lenta: cfr. rischi |
| Costo embedding a freddo | **18,08 s** per 2 chunk (caricamento di `bge-m3` in VRAM) | il primo upload dopo l'avvio sembrerà bloccato |
| `similarity_search_with_score` su match quasi esatto | **0,2041** | è una **distanza**, non una similarità: consegna a P3 |
| Collezioni presenti in `langchain_pg_collection` | solo **`spike`** (3 vettori, lasciata da P0) | da rimuovere in fase 5 |
| Servizi al momento della pianificazione | `db` Up (healthy) su 5434; Ollama con `bge-m3` e `qwen2.5:7b-instruct` | prerequisiti soddisfatti |

### Decisioni di design prese in questa sessione di pianificazione

**1. Gli identificativi dei vettori sono deterministici: `"<document_id>:<ordinal>"`.**
Non UUID generati da `langchain-postgres`. Tre conseguenze, tutte volute:
la reindicizzazione **sovrascrive** invece di duplicare (l'upsert è stato
verificato sul codice della 0.0.17); `DocumentChunk.vector_id` è **noto prima**
della scrittura, quindi le due metà dello schema non hanno bisogno di scambiarsi
informazioni per riconciliarsi; i vettori di un documento sono **ricostruibili
senza le righe `DocumentChunk`**, che è ciò che rende possibile la pulizia in
cascata di RF-08 anche dopo che il `CASCADE` di Django ha già eliminato i chunk.
*Alternativa scartata:* lasciare che `PGVector` generi gli id e leggerli dal
valore di ritorno. Funziona, ma rende la scrittura in pgvector un'operazione da
cui **dipende** la scrittura in Django, e non c'è modo di sapere quali vettori
appartenessero a un documento se le sue righe sono già state cancellate.

**2. L'ordine delle scritture è una scelta di correttezza, e va nella direzione
opposta all'intuizione.** ARCHITECTURE §6.5 dice che «la riconciliazione avviene
in un unico punto, dentro una transazione». Il punto unico è corretto — è
`services/ingestion.py` — ma la transazione **non può essere una sola**:
`PGVector` usa un engine SQLAlchemy con una connessione propria, distinta da
quella di Django (è il prezzo dichiarato di ARCHITECTURE §7.9). Non esiste alcun
`atomic()` che le comprenda entrambe. Si sceglie quindi l'ordine il cui
fallimento è il **meno dannoso**:

> prima i **vettori** (upsert, idempotente), poi le righe Django in `transaction.atomic()`.

Se cade la seconda metà restano *vettori orfani*: invisibili al sistema, e
sovrascritti dalla prossima esecuzione perché gli id sono deterministici.
L'ordine inverso produrrebbe *chunk che puntano a vettori inesistenti*, cioè un
indice che mente al retrieval di P3 — un guasto silenzioso invece di uno
autoriparante. **Questa imprecisione di ARCHITECTURE §6.5 va corretta in T-40**
(cfr. «Consegna a P6»).

**3. `tiktoken` non si installa: la strategia «basata su token» viene rifiutata
con un errore esplicito.** È il debito lasciato aperto da P1. Non è pigrizia:
`tiktoken` implementa i BPE di OpenAI, e usarlo per dimensionare i chunk di
`qwen2.5` e `bge-m3` significherebbe **misurare col metro sbagliato** producendo
un conteggio plausibile e falso — peggio del rifiuto, perché non sarebbe
evidente. Il valore resta nell'enum perché ARCHITECTURE §7.4 lo dichiara come
opzione, e `get_splitter()` solleva `ConfigurazioneNonSupportata` con un messaggio
che dice cosa fare. L'errore **non diventa una pagina 500**: l'ingestione lo
cattura e porta il documento in stato «Fallito» con quel testo (RF-06).
Da dichiarare fra i limiti noti in T-39/T-40.

**4. `EmbeddingProfile.batch_size` viene fatto rispettare dal servizio, non dalla
factory.** `OllamaEmbeddings` non ha alcun parametro di batching e
`PGVector.add_texts()` invia **tutti** i testi in una sola POST a `/api/embed`
(verificato sul sorgente). Un PDF da 400 chunk produrrebbe quindi una singola
richiesta enorme. Il servizio di ingestione affetta i chunk in lotti di
`batch_size` e chiama `add_documents()` una volta per lotto. È ciò che rende il
campo dell'admin **vero** invece che decorativo, e in più dà granularità al
logging del progresso.

**5. `EmbeddingProfile.normalize` non viene applicato dal codice, e l'admin lo
dice.** Misurato in questa sessione: `bge-m3` via Ollama restituisce già vettori a
**norma 1,000000**. Inoltre `PGVector` è configurato con
`DistanceStrategy.COSINE`, che è **invariante alla scala**: normalizzare non può
cambiare alcun risultato di ricerca, per nessun modello. Scrivere un decoratore
`Embeddings` che normalizza sarebbe codice dimostrabilmente inerte — cioè
esattamente il genere di cosa che questo progetto rifiuta. *Alternativa
scartata:* rimuovere il campo, che però è nell'ER di ARCHITECTURE §6.2 e
tornerebbe utile con una distanza L2. Si sceglie la terza via: il campo resta, e
la **descrizione del fieldset nell'admin** spiega che è onorato dal modello e
inerte sotto distanza cosine (fase 4, passo 4.6). Un parametro che non mente non
è un parametro che fa qualcosa: è un parametro che dichiara cosa fa.

**6. `EmbeddingProfile.dimension` diventa un invariante verificato.** Prima di
indicizzare, il servizio calcola l'embedding di una stringa sonda e confronta la
lunghezza con il valore del profilo; se non coincide, si ferma con un errore
leggibile. Costa una chiamata (≈0,8 s a caldo) e chiude il buco dichiarato in
ARCHITECTURE §8.1: la colonna `vector` è senza dimensione, quindi il database
**non** rifiuterebbe vettori di lunghezza sbagliata e l'indice si corromperebbe
in silenzio. Ha un secondo effetto utile: è il punto in cui «Ollama è spento»
diventa un messaggio chiaro **prima** che il documento passi a «In elaborazione».
*Alternativa scartata:* passare `embedding_length=dimension` a `PGVector`, che
tipizzerebbe la colonna come `vector(1024)`. Su questa macchina le tabelle
esistono già senza dimensione, quindi non avrebbe effetto qui ma **cambierebbe lo
schema di un'installazione nuova**: una differenza fra la macchina di sviluppo e
quella di chi valuta è peggio del vincolo mancante.

**7. La deduplica (RF-09) si presidia in due punti, con una sola funzione.** Il
report di P1 ha accertato che il `ModelForm` dell'admin **non valida** `checksum`
perché è in `readonly_fields`: il vincolo `document_checksum_unico_per_kb`
arriverebbe al database come `IntegrityError` 500. Quindi: un
`DocumentAdminForm.clean()` calcola il checksum del file caricato e rifiuta il
duplicato con un errore di campo leggibile — **nessuna riga orfana viene creata** —
e `ingest_document()` ripete il controllo per le vie che non passano dal form
(comando di gestione oggi, API in P4). Una sola implementazione,
`compute_checksum()`, chiamata da entrambi. *Trappola da non calpestare:* leggere
un file caricato dentro `clean()` lascia il puntatore a fine file; senza un
`seek(0)` finale lo storage scriverebbe un PDF **vuoto**.

**8. L'ingestione la innesca `DocumentAdmin.save_model()`, non un signal
`post_save`.** Il servizio salva il `Document` più volte durante il lavoro
(`processing`, poi lo stato finale): un `post_save` si richiamerebbe da sé, e la
prima difesa contro la ricorsione sarebbe un flag di stato — cioè un
`if` messo lì per compensare una scelta sbagliata. `save_model()` è il punto in
cui l'utente ha *chiesto* l'ingestione, ed è anche il posto in cui in P5
diventerà un `enqueue` di una riga.

**9. La cancellazione dei vettori (RF-08) usa `pre_delete` + `post_delete` +
`on_commit`.** Tre pezzi, ognuno per un motivo:
`pre_delete` raccoglie i `vector_id` **finché le righe `DocumentChunk` esistono
ancora** (il collector di Django cancella i figli per primi); `post_delete`
programma la pulizia solo dopo che la cancellazione è andata a buon fine; e
`transaction.on_commit()` la rinvia al commit, perché la connessione di pgvector
è un'altra e un rollback successivo di Django lascerebbe un documento vivo senza
vettori — di nuovo, la direzione sbagliata del guasto (cfr. decisione 2). In caso
di errore nella pulizia si **logga e non si risolleva**: il documento è già
cancellato e una 500 dopo un'operazione riuscita sarebbe una bugia. Gli orfani
sono recuperabili proprio grazie agli id deterministici.

**10. Il servizio non ricrea le righe `DocumentChunk`: le aggiorna per
`(document, ordinal)`.** `DELETE` + `bulk_create` sarebbe più breve, ma
`RetrievedChunk.chunk` è `SET_NULL`: ogni reindicizzazione **azzererebbe lo
storico** delle interrogazioni già registrate, che è proprio il materiale che
RF-16 chiede di conservare. Con `update_or_create` sopravvivono le chiavi
primarie dei chunk il cui ordinale non è cambiato, e si cancellano solo gli
ordinali in eccesso. Effetto collaterale gradito: le due metà dello schema si
scrivono ora con la **stessa** semantica — upsert per `(documento, ordinale)` da
entrambi i lati.

**11. `scripts/spike_rag.py` sopravvive a P2; la collezione `spike` no.** Il
report di P1 li dava entrambi in scadenza a fine P2. La collezione va rimossa
subito — sono tre vettori morti, e lo script la ricrea da sé con
`pre_delete_collection=True`, quindi cancellarla non lo rompe. Lo **script**
invece resta fino alla chiusura di P3: finché `manage.py ask` non esiste, è
l'unica dimostrazione funzionante che il sistema *risponde*, e P2 non la
sostituisce. Cancellarlo ora aprirebbe una finestra senza alcuna prova
end-to-end, senza guadagnare niente.

### Casi limite discussi

| Condizione | Comportamento atteso |
|---|---|
| PDF di sole immagini (scansione) | `PdfSenzaTesto` → stato «Fallito», messaggio che nomina l'OCR come fuori ambito (RF-10, CA-8) |
| PDF corrotto o 0 byte | `PdfIllegibile` → stato «Fallito» (CA-8) |
| PDF protetto da password | `PdfIllegibile`, controllo su `needs_pass` prima di leggere |
| PDF di 50 pagine di cui 3 con testo | Si indicizza. `page_count` = **50** (totale del file), `chunk_count` conta solo i chunk reali |
| Stesso file, stessa KB | Rifiutato dal form con errore di campo; nessuna riga creata (RF-09) |
| Stesso file, KB diversa | **Ammesso**: il vincolo è per base di conoscenza |
| Reindicizzazione con meno chunk della volta precedente | Gli ordinali in eccesso vengono cancellati da pgvector **e** da `DocumentChunk` |
| Ollama spento | Errore leggibile alla verifica della dimensione, prima che il documento passi a «In elaborazione» |
| Profilo di segmentazione con strategia «token» | `ConfigurazioneNonSupportata` → stato «Fallito» con istruzioni, non una 500 |
| Cancellazione di una `KnowledgeBase` con documenti | `CASCADE` sui documenti; ogni documento riceve i propri signal, quindi anche i vettori vengono rimossi |
| Ingestione di un documento già indicizzato | Idempotente: upsert sugli stessi id, `update_or_create` sugli stessi ordinali |

---

## Prerequisiti

Prima di eseguire qualsiasi fase, verificare:

### Repository
- [x] Posizionati in `C:\Users\vjiang\Documents\archetype-lab`
      (verifica: `git rev-parse --show-toplevel`)
- [x] Branch `main`, working tree pulito (verifica: `git status --short` → nessun output)
- [x] Ultimo commit `e81284f` o successivo (verifica: `git log --oneline -1`)

### Ambiente Python
- [x] Virtualenv presente (verifica: `ls .venv/Scripts/python.exe`)
- [x] Le librerie di questa fase sono importabili — **nessuna va installata**
      (verifica: `.venv/Scripts/python.exe -c "import pymupdf, langchain_postgres, langchain_ollama, langchain_text_splitters; print('ok')"`)

### Servizi
- [x] Container del database attivo e sano
      (verifica: `docker compose ps db` → `Up (healthy)`, porta `5434->5432`).
      Se spento: `docker compose up -d db`
- [x] Le quattro migrazioni di P1 sono applicate
      (verifica: `.venv/Scripts/python.exe manage.py showmigrations rag` → quattro `[X]`)
- [x] **Ollama attivo con `bge-m3`** — a differenza di P1, questa fase calcola
      embedding e senza il servizio non è verificabile
      (verifica: `curl -s http://localhost:11434/api/tags` deve elencare `bge-m3`)
- [x] `/health` risponde `ok` su tutti e tre i controlli
      (verifica: avviare `manage.py runserver` e aprire `http://localhost:8000/health`)

### Dati
- [x] La configurazione predefinita di P1 esiste
      (verifica: `.venv/Scripts/python.exe manage.py shell -c "from rag.models import RagPipeline; print(RagPipeline.objects.get(is_default=True).knowledge_base.collection_name)"` → `default`)
- [x] Esiste `samples/manuale-dipendenti.pdf` (verifica: `ls samples/`)

### Non serve, in tutta P2
- **`pytest` non è installato e non va installato.** I test sono P6 (T-36 → T-38).
  Le verifiche di questa fase si fanno con `manage.py shell -c`, con i comandi di
  gestione e con `django.test.Client`, tutti già disponibili.
- **Nessuna nuova dipendenza.** Tutto ciò che serve è già in `requirements.txt`.

---

## Design

### Struttura del progetto al termine di P2

```
archetype-lab/
├── config/                          # invariato
└── rag/
    ├── admin.py                     # modificato — fase 4
    ├── apps.py                      # modificato — fase 5 (ready() per i signal)
    ├── models/                      # INVARIATO: P2 non aggiunge campi
    ├── signals.py                   # NUOVO — fase 5 (RF-08)
    ├── management/                  # NUOVO
    │   ├── __init__.py
    │   └── commands/
    │       ├── __init__.py
    │       └── ingest.py            # NUOVO — fase 4 (T-18)
    └── services/                    # NUOVO — il cuore di P2
        ├── __init__.py
        ├── exceptions.py            # fase 1
        ├── loaders.py               # fase 1 — T-14
        ├── factories.py             # fase 2 — T-15, T-16
        └── ingestion.py             # fase 3 — T-17, T-20
```

**Nessuna migrazione.** P2 non aggiunge né modifica campi: lo schema di P1 è già
completo per l'ingestione. Se `makemigrations` propone qualcosa, è un errore da
capire, non da applicare.

### Il flusso, in una figura

```
Document (pending)
   │
   ├─ compute_checksum ──────────── duplicato? → DocumentoDuplicato → «Fallito»
   ├─ get_splitter(ChunkingProfile)  ─┐
   ├─ get_embeddings(EmbeddingProfile) ├─ factories.py: config → oggetti
   ├─ verify_embedding_dimension      │
   ├─ get_vectorstore(KnowledgeBase) ─┘
   │
   ├─ status = processing  ← salvato e COMMESSO subito: deve essere osservabile
   │
   ├─ load_pdf() ──────────────── pagine (metadata: page, source, document_id)
   ├─ splitter.split_documents() ─ chunk (metadata: + ordinal)
   │
   ├─ PGVector.add_documents(lotto, ids=["<doc>:<ord>", …])   ← PRIMA i vettori
   ├─ PGVector.delete(ids=[ordinali in eccesso])
   │
   └─ transaction.atomic():                                    ← POI Django
        DocumentChunk.update_or_create(document, ordinal)
        chunks.filter(ordinal__gte=n).delete()
        Document → indexed, page_count, chunk_count, checksum,
                   snapshot dei profili, index_fingerprint, indexed_at
```

### Le due metà dello schema, in pratica (ARCHITECTURE §6.3)

| | Proprietario | Connessione | Transazione |
|---|---|---|---|
| `document`, `document_chunk`, … | migrazioni Django | psycopg (Django) | `transaction.atomic()` |
| `langchain_pg_collection`, `langchain_pg_embedding` | `langchain_postgres` | engine SQLAlchemy | propria, non componibile con la prima |

**Il ponte** è `DocumentChunk.vector_id`, e con la decisione 1 è calcolabile da
entrambi i lati senza consultare l'altro.

### Macchina a stati (RF-06)

```
                  ┌──────────────────────────────────────┐
                  ▼                                      │
pending ──▶ processing ──▶ indexed ───(reindicizza)──────┘
                  │            │
                  └──▶ failed ─┴──(reindicizza)──▶ processing
```

Due regole non negoziabili:

1. **`processing` va salvato e commesso da solo**, prima del lavoro. Se stesse
   dentro la transazione finale nessuno lo vedrebbe mai, e l'admin mostrerebbe
   «In attesa» per tutta la durata dell'ingestione. È il campo che RNF-04 chiede
   di rendere ispezionabile.
2. **`failed` porta sempre un `error_message` leggibile da un umano.** Non un
   `repr()` di eccezione. È il criterio CA-8.

### Ciò che questa fase decide e P3 eredita

`factories.py` nasce qui con tre funzioni d'indice — `get_splitter`,
`get_embeddings`, `get_vectorstore` — e in P3 (T-21, T-22) si aggiungono
`get_llm` e `get_retriever`, con la cache invalidata da `post_save`. **In P2 non
si scrive alcuna cache**: l'ingestione costruisce lo store una volta per
documento, che è già il comportamento corretto, e una cache prematura senza
invalidazione sarebbe un bug in attesa.

---

## Modello di esecuzione: una fase = un sub-agente

Le cinque fasi si eseguono in **cinque sub-agenti distinti, in sequenza**. Come in
P0 e P1, ogni passaggio di consegne avviene attraverso **stato persistente** —
file sul disco e contenuto del database — mai attraverso il contesto della
conversazione.

| Da → a | Cosa passa | Come sopravvive al cambio di agente |
|---|---|---|
| 1 → 2 | eccezioni di dominio e loader | `rag/services/exceptions.py`, `loaders.py` |
| 2 → 3 | le tre factory d'indice | `rag/services/factories.py` |
| 3 → 4 | `ingest_document()`, `compute_checksum()`, `vector_id()` | `rag/services/ingestion.py` |
| 4 → 5 | comando e admin che innescano l'ingestione | `rag/management/commands/ingest.py`, `rag/admin.py` |
| 1..4 → 5 | un documento realmente indicizzato | righe in `document`, `document_chunk`, `langchain_pg_embedding` |

**Vincolo per ogni sub-agente:** rileggere dal disco quello che serve. Nessuno può
presumere di aver visto l'output di un altro. Ogni fase termina con un **commit
proprio** e con la propria sezione nel report.

### Report di esecuzione

Ogni fase deve scrivere il proprio esito in
`plans/2026-07-24-2259-P2-plan-report.md`, creandolo se non esiste, con la stessa
struttura usata in P0 e P1: esito, attività coperte, commit, file creati,
scostamenti dal piano, verifica finale, consegna alla fase successiva. Non è
facoltativo: è il meccanismo che rende le fasi separabili.

---

## Fasi di implementazione

### Fase 1: Estrazione del testo dal PDF ed eccezioni di dominio

**Status:** DONE

**Attività di backlog coperte:** T-14

**Stima:** 1 h

**Read first:**
- `ARCHITECTURE.md` §7.10 — perché PyMuPDF diretto, e la frase «il codice di
  estrazione dello spike **non va buttato**, ma promosso in T-14»
- `ARCHITECTURE.md` §8.5 — i PDF scansionati non sono supportati, e il caso va
  rilevato invece di produrre un documento vuoto
- `scripts/spike_rag.py`, righe 57-71 — `load_pdf()`, il codice da promuovere
- `REQUIREMENTS.md` RF-03, RF-10 e CA-8

**Files to modify:**
- `rag/services/__init__.py` (nuovo)
- `rag/services/exceptions.py` (nuovo)
- `rag/services/loaders.py` (nuovo)

**Changes:**

- [x] **1.1** Creare il package dei servizi:
  ```bash
  mkdir -p rag/services
  ```
  ```python
  # rag/services/__init__.py
  """Livello dei servizi: orchestrazione e traduzione della configurazione.

  Nessuna vista e nessun modello importa direttamente LangChain: passano tutti
  da qui. E' la cerniera di ARCHITECTURE §3.
  """
  ```

- [x] **1.2** Creare `rag/services/exceptions.py`. La distinzione che questo file
  introduce regge tutta la gestione degli errori di P2:
  ```python
  """Eccezioni di dominio dell'ingestione.

  Perche' esistono: il servizio deve distinguere un errore ATTESO e imputabile al
  documento o alla sua configurazione — un PDF scansionato, un file corrotto, un
  duplicato, una strategia non realizzabile — da un guasto del sistema. I primi
  finiscono in Document.error_message e portano il documento in stato «Fallito»,
  che e' un esito PREVISTO (RF-06, RF-10, CA-8) e non un incidente; i secondi
  restano eccezioni inattese, vengono comunque persistite (RNF-04) ma anche
  registrate nel log con lo stack completo.

  Tutti i messaggi sono scritti per essere letti da un amministratore
  nell'admin Django, non da uno sviluppatore in un file di log.
  """


  class IngestionError(Exception):
      """Base delle condizioni imputabili al documento o alla configurazione."""


  class PdfIllegibile(IngestionError):
      """Il file non e' apribile come PDF: corrotto, vuoto o protetto."""


  class PdfSenzaTesto(IngestionError):
      """Nessun testo estraibile: probabile scansione senza OCR (RF-10)."""


  class DocumentoDuplicato(IngestionError):
      """Lo stesso file e' gia' presente in questa base di conoscenza (RF-09)."""

      def __init__(self, messaggio: str, *, esistente_id: int | None = None) -> None:
          super().__init__(messaggio)
          self.esistente_id = esistente_id


  class ConfigurazioneNonSupportata(IngestionError):
      """La configurazione e' una riga di database valida ma non realizzabile.

      Esempio: ChunkingProfile.Splitter.TOKEN, che richiederebbe tiktoken.
      Non e' un errore dell'utente ne' un guasto: e' un limite dichiarato.
      """
  ```

- [x] **1.3** Creare `rag/services/loaders.py`. Tre differenze rispetto allo
  spike, tutte necessarie perché il codice viva in un servizio: non stampa e non
  chiama `SystemExit`; distingue *illeggibile* da *senza testo*; restituisce anche
  il conteggio totale delle pagine.
  ```python
  """Estrazione del testo dai PDF (T-14, RF-03, RF-10).

  Promosso da scripts/spike_rag.py::load_pdf(), che ARCHITECTURE §7.10 indica
  come l'unico codice dello spike da non buttare via.

  Si importa `pymupdf` e non `fitz`: dalla 1.24 il nome canonico del modulo e'
  `pymupdf` e `fitz` resta come alias di compatibilita'. Verificato sulla 1.28.0
  installata.
  """

  from __future__ import annotations

  from dataclasses import dataclass

  import pymupdf
  from langchain_core.documents import Document as LCDocument

  from .exceptions import PdfIllegibile, PdfSenzaTesto


  @dataclass(frozen=True)
  class PdfEstratto:
      """Esito dell'estrazione.

      page_count e' il totale delle pagine del FILE, non delle pagine con testo:
      un PDF di 50 pagine di cui 3 contengono testo va indicizzato, ma l'admin
      deve mostrare 50 (criterio CA-2).
      """

      pagine: list[LCDocument]
      page_count: int


  def load_pdf(path: str, *, metadata_extra: dict | None = None) -> PdfEstratto:
      """Estrae il testo pagina per pagina conservando il numero di pagina.

      Il numero di pagina e' 1-based perche' finisce nelle citazioni mostrate
      all'utente (RF-13): «pagina 0» non si scrive in una risposta.

      Solleva:
          PdfIllegibile: file inesistente, corrotto, vuoto o protetto.
          PdfSenzaTesto: nessuna pagina contiene testo estraibile (RF-10).
      """
      try:
          documento = pymupdf.open(path)
      except pymupdf.FileNotFoundError as exc:
          # ATTENZIONE: pymupdf.FileNotFoundError OMBREGGIA l'omonima builtin e
          # NON la sottoclassa — deriva da RuntimeError. Verificato sulla 1.28.0:
          # scrivere `except FileNotFoundError` qui NON la catturerebbe, e
          # l'eccezione sfuggirebbe al servizio come guasto inatteso. L'ordine
          # delle clausole conta: va prima di FileDataError.
          raise PdfIllegibile(f"File non trovato: {path}") from exc
      except pymupdf.FileDataError as exc:
          # EmptyFileError e' sottoclasse di FileDataError (verificato), quindi
          # una sola clausola copre file corrotto e file di zero byte. Entrambe
          # derivano da RuntimeError e non da OSError: catturarle per nome e'
          # l'unico modo di distinguerle da un guasto vero.
          raise PdfIllegibile(
              f"Il file non e' un PDF leggibile ({type(exc).__name__}). "
              "Verificare che non sia corrotto o troncato."
          ) from exc

      with documento:
          if documento.needs_pass:
              raise PdfIllegibile(
                  "Il PDF e' protetto da password: non e' possibile estrarne il testo."
              )

          page_count = documento.page_count
          pagine: list[LCDocument] = []
          for numero, pagina in enumerate(documento, start=1):
              testo = pagina.get_text().strip()
              if not testo:
                  continue
              metadata = {"page": numero, "source": path}
              if metadata_extra:
                  metadata.update(metadata_extra)
              pagine.append(LCDocument(page_content=testo, metadata=metadata))

      if not pagine:
          raise PdfSenzaTesto(
              f"Nessun testo estraibile dalle {page_count} pagine del documento. "
              "E' probabilmente una scansione senza OCR: l'OCR e' dichiarato fuori "
              "ambito (REQUIREMENTS §8), quindi questo file non e' indicizzabile."
          )
      return PdfEstratto(pagine=pagine, page_count=page_count)
  ```

**Verify:**
```bash
# 1. Il PDF di prova: 3 pagine con testo, page_count 3, metadata con la pagina
.venv/Scripts/python.exe -c "
from rag.services.loaders import load_pdf
e = load_pdf('samples/manuale-dipendenti.pdf', metadata_extra={'document_id': 42})
print('pagine con testo:', len(e.pagine), '| page_count:', e.page_count)
print('metadata:', e.pagine[0].metadata)
assert e.page_count == 3 and len(e.pagine) == 3
assert e.pagine[0].metadata['page'] == 1 and e.pagine[0].metadata['document_id'] == 42
print('OK')
"

# 2. PDF corrotto e PDF vuoto -> PdfIllegibile (CA-8)
.venv/Scripts/python.exe -c "
import tempfile, os
from rag.services.loaders import load_pdf
from rag.services.exceptions import PdfIllegibile
for nome, contenuto in (('corrotto.pdf', b'non sono un pdf'), ('vuoto.pdf', b'')):
    p = os.path.join(tempfile.gettempdir(), nome)
    open(p, 'wb').write(contenuto)
    try:
        load_pdf(p); print('FALLITO: nessuna eccezione per', nome)
    except PdfIllegibile as exc:
        print(nome, '->', exc)
    os.remove(p)
try:
    load_pdf('samples/inesistente.pdf'); print('FALLITO')
except PdfIllegibile as exc:
    print('inesistente ->', exc)
"

# 3. PDF senza testo -> PdfSenzaTesto (RF-10). Si genera una pagina vuota con PyMuPDF
.venv/Scripts/python.exe -c "
import tempfile, os, pymupdf
from rag.services.loaders import load_pdf
from rag.services.exceptions import PdfSenzaTesto
p = os.path.join(tempfile.gettempdir(), 'senzatesto.pdf')
d = pymupdf.open(); d.new_page(); d.new_page(); d.save(p); d.close()
try:
    load_pdf(p); print('FALLITO: nessuna eccezione')
except PdfSenzaTesto as exc:
    print('OK ->', exc)
os.remove(p)
"
```
Nota: i comandi qui sopra non richiedono `manage.py`, ma `load_pdf` importa
`langchain_core`, non Django: girano come sono. Se un import di Django comparisse,
usare `manage.py shell -c` al loro posto.

**Phase Complete When:**
- [x] `rag/services/{__init__,exceptions,loaders}.py` esistono
- [x] Le tre verifiche sopra passano, **inclusi i tre casi di errore**
- [x] `.venv/Scripts/python.exe manage.py check` non riporta problemi
- [x] `scripts/spike_rag.py` **non è stato modificato né cancellato** (decisione 11)
- [x] Commit: `P2: estrazione del testo dai PDF ed eccezioni di dominio (T-14)`
- [x] Sezione del report scritta

---

### Fase 2: Factory della configurazione d'indice

**Status:** DONE

**Attività di backlog coperte:** T-15, T-16

**Stima:** 1 h 45

**Read first:**
- `ARCHITECTURE.md` §3 (il factory layer come cerniera), §7.4 (chunking), §7.9
  (`PGVector`, due ORM e due driver), §8.1 (la dimensione è un vincolo
  applicativo)
- `rag/models/profiles.py` — `ChunkingProfile` (enum `Splitter`, `separators`),
  `EmbeddingProfile` (enum `Provider`, `dimension`, `normalize`, `batch_size`)
- `rag/models/domain.py` — `KnowledgeBase.collection_name` e il suo `help_text`
- `scripts/spike_rag.py`, righe 47-54 e 85-95 — `connection_string()` e la
  costruzione dello store da imitare
- Di questo piano: le decisioni **3, 4, 5, 6** e la tabella dei dati di realtà

**Files to modify:**
- `rag/services/factories.py` (nuovo)

**Changes:**

- [x] **2.1** Creare `rag/services/factories.py` con l'intestazione e la stringa
  di connessione:
  ```python
  """Configurazione (righe di database) → oggetti LangChain.

  E' la cerniera dell'architettura (ARCHITECTURE §3): l'unico punto del progetto
  che traduce profili in oggetti. Tutto il resto del codice riceve oggetti gia'
  costruiti e non sa da dove vengano i parametri. E' il motivo per cui una
  modifica dall'admin ha effetto senza toccare il codice.

  In P2 esistono solo le tre factory della configurazione d'INDICE. get_llm() e
  get_retriever() sono T-21 e T-22, in P3, insieme alla cache invalidata da
  post_save: NON anticiparle qui.
  """

  from __future__ import annotations

  from urllib.parse import quote

  from django.conf import settings
  from langchain_core.embeddings import Embeddings
  from langchain_ollama import OllamaEmbeddings
  from langchain_postgres import PGVector
  from langchain_text_splitters import RecursiveCharacterTextSplitter, TextSplitter

  from ..models import ChunkingProfile, EmbeddingProfile, KnowledgeBase
  from .exceptions import ConfigurazioneNonSupportata


  def connection_string() -> str:
      """URL SQLAlchemy verso lo STESSO database di Django.

      Due ORM e due driver sullo stesso database sono il prezzo dichiarato di
      ARCHITECTURE §7.9, non una svista. Le credenziali vanno quotate: una
      password contenente «@», «/» o «:» romperebbe l'URL in silenzio, e il
      messaggio d'errore parlerebbe di host inesistenti.
      """
      d = settings.DATABASES["default"]
      utente = quote(str(d["USER"]), safe="")
      password = quote(str(d["PASSWORD"]), safe="")
      return (
          f"postgresql+psycopg://{utente}:{password}"
          f"@{d['HOST']}:{d['PORT']}/{d['NAME']}"
      )
  ```

- [x] **2.2** Aggiungere `get_splitter()` (T-15). Due punti delicati: i separatori
  vuoti e il rifiuto della strategia a token.
  ```python
  def get_splitter(profile: ChunkingProfile) -> TextSplitter:
      """Costruisce lo splitter descritto dal profilo (RF-19).

      Solleva:
          ConfigurazioneNonSupportata: strategia dichiarata nell'enum ma non
              realizzabile in questa installazione.
      """
      if profile.splitter == ChunkingProfile.Splitter.RECURSIVE:
          parametri: dict = {
              "chunk_size": profile.chunk_size,
              "chunk_overlap": profile.chunk_overlap,
              # Aggiunge start_index ai metadata: e' l'offset del chunk dentro la
              # pagina, utile all'estratto delle citazioni di P3 (RF-13).
              "add_start_index": True,
          }
          # ATTENZIONE: separators vuoto significa «usa i predefiniti dello
          # splitter», non «nessun separatore». Passare [] esplicitamente
          # disattiverebbe ogni confine di paragrafo e produrrebbe un taglio
          # cieco a chunk_size caratteri — l'opposto di cio' che
          # ARCHITECTURE §7.4 sceglie. Il campo e' blank=True proprio per
          # significare «predefiniti».
          if profile.separators:
              parametri["separators"] = list(profile.separators)
          return RecursiveCharacterTextSplitter(**parametri)

      if profile.splitter == ChunkingProfile.Splitter.TOKEN:
          # Limite dichiarato, non dimenticanza. tiktoken implementa i BPE di
          # OpenAI: usarlo per dimensionare i chunk di qwen2.5 e bge-m3
          # significherebbe misurare col metro sbagliato, producendo un
          # conteggio plausibile e falso. Meglio rifiutare in modo esplicito.
          raise ConfigurazioneNonSupportata(
              f"Il profilo di segmentazione «{profile.name}» usa la strategia "
              "«basato su token», che questa installazione non realizza: "
              "TokenTextSplitter richiede il pacchetto tiktoken, deliberatamente "
              "escluso dalle dipendenze. Selezionare «Recursive character» nel "
              "profilo di segmentazione."
          )

      raise ConfigurazioneNonSupportata(
          f"Strategia di segmentazione sconosciuta: {profile.splitter!r}."
      )
  ```

- [x] **2.3** Aggiungere `get_embeddings()` e la verifica della dimensione (T-16):
  ```python
  def get_embeddings(profile: EmbeddingProfile) -> Embeddings:
      """Costruisce il modello di embedding descritto dal profilo.

      L'endpoint NON viene dal profilo: e' settings.OLLAMA_BASE_URL. E' una
      scelta di P1 (decisione 2 del piano di P1) e di ARCHITECTURE §9: il testo
      dei chunk deve avere un solo punto di uscita possibile, e renderlo
      configurabile ne aggiungerebbe un secondo senza alcun beneficio.

      normalize NON viene applicato: misurato in fase di pianificazione, bge-m3
      restituisce gia' vettori a norma 1,000000, e PGVector usa distanza cosine,
      che e' invariante alla scala. Un decoratore che normalizza sarebbe codice
      dimostrabilmente inerte. La descrizione nell'admin lo dichiara.

      dimension non viene passato al costruttore: cfr. verify_embedding_dimension.
      """
      if profile.provider != EmbeddingProfile.Provider.OLLAMA:
          # Il provider «huggingface» dell'enum richiederebbe
          # sentence-transformers e quindi torch (~2,5 GB), che
          # ARCHITECTURE §7.3 esclude esplicitamente. Il valore resta nell'enum
          # come alternativa documentata, non come opzione attivabile.
          raise ConfigurazioneNonSupportata(
              f"Il profilo di embedding «{profile.name}» usa il provider "
              f"«{profile.get_provider_display()}», che questa installazione non "
              "realizza: richiederebbe sentence-transformers e torch, esclusi per "
              "scelta (ARCHITECTURE §7.3). Selezionare il provider «Ollama»."
          )
      return OllamaEmbeddings(
          model=profile.model_name,
          base_url=settings.OLLAMA_BASE_URL,
      )


  def verify_embedding_dimension(embeddings: Embeddings, profile: EmbeddingProfile) -> int:
      """Verifica che il modello produca vettori della dimensione dichiarata.

      Serve perche' la colonna creata da langchain-postgres e' `vector` SENZA
      dimensione (verificato in P0): il database NON rifiuterebbe un vettore di
      lunghezza sbagliata, e l'indice si corromperebbe in silenzio. Il controllo
      trasforma EmbeddingProfile.dimension da campo descrittivo in invariante
      verificato (ARCHITECTURE §8.1).

      Costa una chiamata di embedding (≈0,8 s a caldo, fino a ~18 s a freddo) e
      ha un secondo effetto utile: e' il punto in cui «Ollama e' spento» diventa
      un messaggio leggibile, prima che il documento passi a «In elaborazione».
      """
      try:
          sonda = embeddings.embed_query("verifica della dimensione del vettore")
      except Exception as exc:  # noqa: BLE001 - qualunque guasto va tradotto
          raise ConfigurazioneNonSupportata(
              f"Il servizio di embedding non risponde su {settings.OLLAMA_BASE_URL}: "
              f"{exc}. Verificare che Ollama sia in esecuzione e che il modello "
              f"«{profile.model_name}» sia stato scaricato (`ollama pull "
              f"{profile.model_name}`)."
          ) from exc

      if len(sonda) != profile.dimension:
          raise ConfigurazioneNonSupportata(
              f"Il modello «{profile.model_name}» produce vettori di "
              f"{len(sonda)} dimensioni, ma il profilo «{profile.name}» dichiara "
              f"{profile.dimension}. Correggere il profilo oppure cambiare "
              "modello: indicizzare con questa incoerenza renderebbe il recupero "
              "inaffidabile senza alcun errore visibile."
          )
      return len(sonda)
  ```

- [x] **2.4** Aggiungere `get_vectorstore()` (T-16). La docstring documenta un
  effetto collaterale scoperto leggendo il sorgente della 0.0.17:
  ```python
  def get_vectorstore(
      knowledge_base: KnowledgeBase, *, embeddings: Embeddings | None = None
  ) -> PGVector:
      """Costruisce il vector store della base di conoscenza.

      ATTENZIONE — costruire questo oggetto ha EFFETTI COLLATERALI. Verificato
      sul sorgente della 0.0.17: PGVector.__post_init__ esegue DDL a ogni
      costruzione — create_tables_if_not_exists() e create_collection() — su una
      connessione SQLAlchemy distinta da quella di Django. Due conseguenze
      operative:

      1. si costruisce UNA volta per ingestione, mai dentro un ciclo sui lotti;
      2. create_extension=False, perche' la migrazione 0001 ha gia' installato
         l'estensione e ripetere CREATE EXTENSION a ogni costruzione e' DDL
         inutile.

      Il lato positivo di quel DDL: la collezione di una base di conoscenza nuova
      viene creata alla prima ingestione, senza bisogno di alcun passo esplicito.

      embeddings e' iniettabile per non pagare due volte la costruzione quando il
      chiamante lo ha gia'.
      """
      return PGVector(
          embeddings=embeddings or get_embeddings(knowledge_base.embedding_profile),
          collection_name=knowledge_base.collection_name,
          connection=connection_string(),
          use_jsonb=True,
          create_extension=False,
      )
  ```

**Verify:**
```bash
# 1. Lo splitter rispetta il profilo, e cambiare il profilo cambia il risultato (RF-19)
.venv/Scripts/python.exe manage.py shell -c "
from rag.models import ChunkingProfile
from rag.services.factories import get_splitter
from rag.services.loaders import load_pdf
p = ChunkingProfile.objects.get(splitter='recursive')
pagine = load_pdf('samples/manuale-dipendenti.pdf').pagine
s = get_splitter(p)
print(type(s).__name__, '| chunk_size', s._chunk_size, '| overlap', s._chunk_overlap)
assert s._chunk_size == p.chunk_size and s._chunk_overlap == p.chunk_overlap
base = len(s.split_documents(pagine))
# CORRETTO IN ESECUZIONE: il piano prescriveva 200/20, ma le tre pagine del PDF
# di prova sono lunghe 166, 147 e 126 caratteri — una finestra di 200 le contiene
# ancora intere e il conteggio resta 3. 100/10 e' il primo valore sotto la pagina
# piu' corta: 3 -> 6 chunk.
p.chunk_size, p.chunk_overlap = 100, 10
ridotto = len(get_splitter(p).split_documents(pagine))
print('chunk a 800/120:', base, '| a 100/10:', ridotto)
assert ridotto > base, 'un chunk_size minore deve produrre piu chunk'
print('start_index nei metadata:', 'start_index' in s.split_documents(pagine)[0].metadata)
print('OK')  # p NON viene salvato: nessuna modifica al database
"

# 2. La strategia a token viene rifiutata con un messaggio utile (decisione 3)
.venv/Scripts/python.exe manage.py shell -c "
from rag.models import ChunkingProfile
from rag.services.factories import get_splitter
from rag.services.exceptions import ConfigurazioneNonSupportata
p = ChunkingProfile.objects.first(); p.splitter = 'token'
try:
    get_splitter(p); print('FALLITO: nessuna eccezione')
except ConfigurazioneNonSupportata as exc:
    print('OK ->', exc)
"

# 3. Il provider huggingface viene rifiutato
.venv/Scripts/python.exe manage.py shell -c "
from rag.models import EmbeddingProfile
from rag.services.factories import get_embeddings
from rag.services.exceptions import ConfigurazioneNonSupportata
p = EmbeddingProfile.objects.first(); p.provider = 'huggingface'
try:
    get_embeddings(p); print('FALLITO')
except ConfigurazioneNonSupportata as exc:
    print('OK ->', exc)
"

# 4. Embedding reali: dimensione verificata e coerente col profilo (RICHIEDE Ollama)
.venv/Scripts/python.exe manage.py shell -c "
from rag.models import EmbeddingProfile
from rag.services.factories import get_embeddings, verify_embedding_dimension
p = EmbeddingProfile.objects.first()
e = get_embeddings(p)
print('dimensione misurata:', verify_embedding_dimension(e, p), '| dichiarata:', p.dimension)
p.dimension = 999
try:
    verify_embedding_dimension(e, p); print('FALLITO: incoerenza non rilevata')
except Exception as exc:
    print('OK, incoerenza rilevata ->', exc)
"

# 5. Lo store si costruisce e crea la collezione della KB predefinita
.venv/Scripts/python.exe manage.py shell -c "
from django.db import connection
from rag.models import KnowledgeBase
from rag.services.factories import get_vectorstore, connection_string
print('URL:', connection_string().replace('rag:rag', 'rag:***'))
kb = KnowledgeBase.objects.get(collection_name='default')
get_vectorstore(kb)
with connection.cursor() as c:
    c.execute('select name from langchain_pg_collection order by name')
    print('collezioni:', [r[0] for r in c.fetchall()])
"
```
Atteso al punto 5: `['default', 'spike']` — la collezione `default` viene creata
qui dal DDL del costruttore. È il comportamento voluto, non un effetto da
correggere.

**Phase Complete When:**
- [x] `rag/services/factories.py` esiste con `connection_string`, `get_splitter`,
      `get_embeddings`, `verify_embedding_dimension`, `get_vectorstore`
- [x] Le cinque verifiche passano, incluse le tre di rifiuto
- [x] La collezione `default` esiste in `langchain_pg_collection`
- [x] **Nessuna** funzione `get_llm` o `get_retriever` è stata scritta (sono P3)
- [x] **Nessuna** cache è stata introdotta (è T-21)
- [x] `manage.py makemigrations --check --dry-run` → «No changes detected»
- [x] Commit: `P2: factory di segmentazione, embedding e vector store (T-15, T-16)`
- [x] Sezione del report scritta

---

### Fase 3: Servizio di ingestione e macchina a stati

**Status:** DONE

**Attività di backlog coperte:** T-17, e la parte di T-20 relativa alla deduplica

**Stima:** 2 h

**Read first:**
- **ARCHITECTURE.md §6.3 e §6.5** — le due metà dello schema e la duplicazione del
  testo. Sono la specifica di questo file
- `ARCHITECTURE.md` §4 (flusso di ingestione), §7.9
- `REQUIREMENTS.md` §6 UC-1 con le estensioni 3a, 3b, 1a
- `rag/models/domain.py` — `Document` (stati, snapshot, `index_fingerprint`,
  vincolo parziale sul checksum), `DocumentChunk` (vincolo su `(document, ordinal)`)
- `rag/services/factories.py` e `loaders.py`, appena scritti
- Di questo piano: le decisioni **1, 2, 4, 6, 7, 10** e il diagramma del flusso

**Files to modify:**
- `rag/services/ingestion.py` (nuovo)

**Changes:**

- [x] **3.1** Creare `rag/services/ingestion.py` con l'intestazione. Il commento
  sull'ordine delle scritture non è decorativo: è la ragione per cui il file è
  scritto così.
  ```python
  """Orchestrazione dell'ingestione: PDF → chunk → vettori → righe di database.

  E' il punto UNICO in cui le due meta' dello schema si riconciliano
  (ARCHITECTURE §6.3, §6.5).

  L'ORDINE DELLE SCRITTURE E' UNA SCELTA DI CORRETTEZZA, NON DI STILE.

  PGVector usa un engine SQLAlchemy con una connessione propria, distinta da
  quella di Django: non esiste alcun transaction.atomic() che comprenda
  entrambe. ARCHITECTURE §6.5 parla di «una transazione», ed e' un'imprecisione
  da correggere in T-40. Dovendo scegliere quale meta' puo' restare indietro, si
  scrive PRIMA in pgvector e POI in Django:

  - se cade la scrittura Django restano VETTORI ORFANI: invisibili al sistema, e
    sovrascritti dalla prossima esecuzione perche' gli id sono deterministici.
    Il guasto si ripara da solo;
  - l'ordine inverso lascerebbe CHUNK CHE PUNTANO A VETTORI INESISTENTI, cioe' un
    indice che mente al retrieval di P3. Un guasto silenzioso e permanente.

  Gli id dei vettori sono deterministici — «<document_id>:<ordinal>» — e questo
  e' cio' che rende possibili sia l'idempotenza (l'upsert di
  langchain-postgres 0.0.17 e' ON CONFLICT (id) DO UPDATE, verificato sul
  sorgente) sia la pulizia in cascata di RF-08, che deve poter ricostruire gli id
  dei vettori quando le righe DocumentChunk sono GIA' state cancellate.
  """

  from __future__ import annotations

  import hashlib
  import logging
  import time
  from dataclasses import dataclass
  from pathlib import Path

  from django.db import transaction
  from django.utils import timezone

  from ..models import Document, DocumentChunk
  from .exceptions import DocumentoDuplicato, IngestionError, PdfSenzaTesto
  from .factories import (
      get_embeddings,
      get_splitter,
      get_vectorstore,
      verify_embedding_dimension,
  )
  from .loaders import load_pdf

  logger = logging.getLogger(__name__)
  ```

- [x] **3.2** Aggiungere le tre funzioni di supporto. `vector_id()` è il ponte fra
  le due metà dello schema e non va reimplementata altrove:
  ```python
  def vector_id(document_id: int, ordinal: int) -> str:
      """Identificativo del vettore in langchain_pg_embedding.

      Deterministico di proposito (cfr. l'intestazione del modulo). Ricalcolabile
      da entrambe le meta' dello schema senza consultare l'altra: e' cio' che
      rende la reindicizzazione idempotente e la pulizia dei vettori possibile a
      chunk gia' cancellati.

      langchain_pg_embedding.id e' `character varying`, verificato in P0: una
      stringa e' un valore legittimo, non un UUID mascherato.
      """
      return f"{document_id}:{ordinal}"


  def compute_checksum(file) -> str:
      """SHA-256 del file, a blocchi (RF-09).

      A blocchi e non con read(): un PDF puo' pesare decine di MB e non va
      caricato tutto in memoria.

      La funzione RIPRISTINA lo stato di partenza del file, e nessuna delle due
      metà e' cosmetica:

      - se il file era APERTO — il caso del form dell'admin, che valida un upload
        prima che lo storage lo scriva — si riporta il puntatore a zero. Senza il
        seek(0) finale si salverebbe un PDF di ZERO BYTE;
      - se era CHIUSO — il caso di un FieldFile appena letto dal database — lo si
        richiude. Su Windows un handle aperto su quel percorso fa fallire con
        PermissionError [WinError 32] la cancellazione o la sostituzione del file
        da parte di un'ALTRA istanza di FieldFile. Osservato in fase di
        pianificazione.

      `FieldFile.closed` e' True finche' nessuno legge; il seek() apre il file e
      close() lo richiude. Verificato.
      """
      era_chiuso = getattr(file, "closed", False)
      digest = hashlib.sha256()
      file.seek(0)
      for blocco in file.chunks():
          digest.update(blocco)
      if era_chiuso:
          file.close()
      else:
          file.seek(0)
      return digest.hexdigest()


  def trova_duplicato(knowledge_base_id: int, checksum: str, *, escludi_id=None):
      """Restituisce il Document con lo stesso checksum nella stessa KB, se c'e'.

      Il vincolo di unicita' e' per base di conoscenza, non globale: lo stesso PDF
      in due raccolte distinte e' legittimo (ARCHITECTURE §6.4).
      """
      qs = Document.objects.filter(knowledge_base_id=knowledge_base_id, checksum=checksum)
      if escludi_id is not None:
          qs = qs.exclude(pk=escludi_id)
      return qs.first()
  ```

- [x] **3.3** Aggiungere il tipo di esito e lo scheletro di `ingest_document()`
  con la gestione degli errori. La distinzione fra `IngestionError` e il resto è
  ciò che separa un esito previsto da un incidente:
  ```python
  @dataclass(frozen=True)
  class EsitoIngestione:
      document_id: int
      page_count: int
      chunk_count: int
      vettori_rimossi: int
      durata_s: float

      def __str__(self) -> str:
          return (
              f"documento {self.document_id}: {self.chunk_count} segmenti su "
              f"{self.page_count} pagine in {self.durata_s:.1f}s"
              + (f" ({self.vettori_rimossi} vettori obsoleti rimossi)" if self.vettori_rimossi else "")
          )


  def ingest_document(document: Document) -> EsitoIngestione:
      """Indicizza (o reindicizza) un documento. Sincrona in P2.

      Idempotente: rieseguirla sullo stesso documento sovrascrive i vettori e
      aggiorna le righe DocumentChunk, senza duplicare nulla. E' cio' che rende
      T-19 («reindicizza») una semplice rilettura di questa funzione.

      L'asincronia e' P5 (T-32): fino a quel momento la chiamata BLOCCA il
      chiamante — la richiesta dell'admin o il comando di gestione — per tutta la
      durata dell'indicizzazione. E' un limite noto e dichiarato, non una
      dimenticanza.

      Solleva:
          IngestionError: condizione imputabile al documento o alla sua
              configurazione. Lo stato «Fallito» e' GIA' stato persistito quando
              l'eccezione arriva al chiamante: il rilancio serve solo a
              informarlo.
          Exception: guasto inatteso. Anch'esso persistito (RNF-04), e in piu'
              registrato nel log con lo stack completo.
      """
      inizio = time.perf_counter()
      try:
          return _esegui_ingestione(document, inizio)
      except IngestionError as exc:
          _marca_fallito(document, str(exc))
          logger.warning("Ingestione del documento %s rifiutata: %s", document.pk, exc)
          raise
      except Exception as exc:
          # Non e' una condizione prevista: il messaggio persistito deve restare
          # leggibile, ma lo stack va nel log perche' serve a chi sviluppa.
          _marca_fallito(
              document,
              f"Errore inatteso durante l'indicizzazione "
              f"({type(exc).__name__}): {exc}",
          )
          logger.exception("Ingestione del documento %s fallita", document.pk)
          raise


  def _marca_fallito(document: Document, messaggio: str) -> None:
      """Persiste lo stato «Fallito» e il motivo (RF-06, RNF-04, CA-8).

      Fuori da qualunque transazione del chiamante: se fosse dentro quella che
      sta fallendo, il rollback cancellerebbe anche la spiegazione del
      fallimento.
      """
      document.status = Document.Status.FAILED
      document.error_message = messaggio
      document.save(update_fields=["status", "error_message"])
  ```

- [x] **3.4** Aggiungere il corpo dell'ingestione. È il passo centrale della fase:
  ```python
  def _esegui_ingestione(document: Document, inizio: float) -> EsitoIngestione:
      kb = document.knowledge_base
      profilo_embedding = kb.embedding_profile
      profilo_chunking = kb.chunking_profile

      # --- 1. Deduplica (RF-09), prima di qualunque lavoro costoso -----------
      checksum = compute_checksum(document.file)
      esistente = trova_duplicato(kb.pk, checksum, escludi_id=document.pk)
      if esistente is not None:
          raise DocumentoDuplicato(
              f"Questo file e' gia' presente nella base di conoscenza "
              f"«{kb.name}» come documento {esistente.pk} "
              f"({esistente.original_filename or esistente.file.name}). "
              "Lo stesso contenuto non viene indicizzato due volte.",
              esistente_id=esistente.pk,
          )

      # --- 2. Configurazione → oggetti, e verifica dei presupposti ----------
      # Prima dello stato «In elaborazione»: una configurazione irrealizzabile o
      # un Ollama spento devono produrre un errore chiaro senza far passare il
      # documento per uno stato che non ha mai avuto.
      splitter = get_splitter(profilo_chunking)
      embeddings = get_embeddings(profilo_embedding)
      verify_embedding_dimension(embeddings, profilo_embedding)
      store = get_vectorstore(kb, embeddings=embeddings)

      # --- 3. «In elaborazione», salvato e commesso DA SOLO -----------------
      # Se stesse nella transazione finale nessuno lo vedrebbe mai, e l'admin
      # mostrerebbe «In attesa» per tutta la durata del lavoro (RNF-04).
      document.status = Document.Status.PROCESSING
      document.error_message = ""
      document.save(update_fields=["status", "error_message"])

      # --- 4. Estrazione e segmentazione ------------------------------------
      # document.file.path presuppone uno storage su filesystem locale, che e'
      # quello configurato (MEDIA_ROOT). Con uno storage remoto servirebbe
      # scrivere il file in un temporaneo: fuori ambito.
      estratto = load_pdf(
          document.file.path,
          metadata_extra={
              "document_id": document.pk,
              "knowledge_base_id": kb.pk,
              "documento": document.original_filename or Path(document.file.name).name,
          },
      )
      pezzi = splitter.split_documents(estratto.pagine)
      if not pezzi:
          # Difesa: pagine con testo ma nessun chunk significa configurazione
          # patologica, non PDF scansionato. Il messaggio deve dirlo.
          raise PdfSenzaTesto(
              f"La segmentazione non ha prodotto alcun segmento dalle "
              f"{len(estratto.pagine)} pagine con testo. Verificare il profilo "
              f"«{profilo_chunking.name}»."
          )

      for ordinale, pezzo in enumerate(pezzi):
          pezzo.metadata["ordinal"] = ordinale

      # --- 5. PRIMA i vettori: upsert a lotti di batch_size -----------------
      # OllamaEmbeddings non sa batchare e PGVector.add_texts() invia TUTTI i
      # testi in una sola POST a /api/embed (verificato sul sorgente della
      # 0.0.17). L'affettamento e' quindi l'unico modo di far rispettare
      # EmbeddingProfile.batch_size, che altrimenti sarebbe un campo decorativo.
      dimensione_lotto = max(1, profilo_embedding.batch_size)
      for da in range(0, len(pezzi), dimensione_lotto):
          lotto = pezzi[da : da + dimensione_lotto]
          store.add_documents(
              lotto,
              ids=[vector_id(document.pk, da + i) for i in range(len(lotto))],
          )
          logger.debug(
              "Documento %s: indicizzati %s/%s segmenti",
              document.pk, da + len(lotto), len(pezzi),
          )

      # --- 6. Vettori obsoleti di una indicizzazione precedente piu' lunga --
      ordinali_precedenti = set(document.chunks.values_list("ordinal", flat=True))
      ordinali_precedenti.update(range(document.chunk_count))
      obsoleti = sorted(o for o in ordinali_precedenti if o >= len(pezzi))
      if obsoleti:
          store.delete(ids=[vector_id(document.pk, o) for o in obsoleti])

      # --- 7. POI Django, in una transazione --------------------------------
      with transaction.atomic():
          # update_or_create e non delete + bulk_create: RetrievedChunk.chunk e'
          # SET_NULL, quindi ricreare le righe azzererebbe lo storico delle
          # interrogazioni (RF-16). Cosi' i chunk il cui ordinale non e'
          # cambiato conservano la chiave primaria. Le due meta' dello schema
          # finiscono per usare la STESSA semantica: upsert per
          # (documento, ordinale).
          for ordinale, pezzo in enumerate(pezzi):
              DocumentChunk.objects.update_or_create(
                  document=document,
                  ordinal=ordinale,
                  defaults={
                      "page_number": pezzo.metadata["page"],
                      "content": pezzo.page_content,
                      "char_count": len(pezzo.page_content),
                      "vector_id": vector_id(document.pk, ordinale),
                  },
              )
          document.chunks.filter(ordinal__gte=len(pezzi)).delete()

          document.status = Document.Status.INDEXED
          document.checksum = checksum
          document.page_count = estratto.page_count
          document.chunk_count = len(pezzi)
          document.error_message = ""
          if not document.original_filename:
              # Debito lasciato aperto da P1: il campo esiste e nessuno lo
              # valorizzava.
              document.original_filename = Path(document.file.name).name
          document.indexed_embedding_profile = profilo_embedding
          document.indexed_chunking_profile = profilo_chunking
          document.index_fingerprint = kb.index_fingerprint()
          document.indexed_at = timezone.now()
          document.save(
              update_fields=[
                  "status", "checksum", "page_count", "chunk_count",
                  "error_message", "original_filename",
                  "indexed_embedding_profile", "indexed_chunking_profile",
                  "index_fingerprint", "indexed_at",
              ]
          )

      esito = EsitoIngestione(
          document_id=document.pk,
          page_count=estratto.page_count,
          chunk_count=len(pezzi),
          vettori_rimossi=len(obsoleti),
          durata_s=time.perf_counter() - inizio,
      )
      logger.info("Ingestione completata — %s", esito)
      return esito
  ```

**Verify:**
```bash
# 1. Ingestione reale del PDF di prova, dallo stato pending all'indicizzato (CA-2)
.venv/Scripts/python.exe manage.py shell -c "
from django.core.files import File
from rag.models import Document, KnowledgeBase, RagPipeline
from rag.services.ingestion import ingest_document
kb = RagPipeline.objects.get(is_default=True).knowledge_base
d = Document(knowledge_base=kb)
with open('samples/manuale-dipendenti.pdf','rb') as f:
    d.file.save('manuale-dipendenti.pdf', File(f), save=False)
d.save()
print('creato', d.pk, 'stato', d.status)
esito = ingest_document(d)
d.refresh_from_db()
print('esito:', esito)
print('stato:', d.status, '| pagine:', d.page_count, '| segmenti:', d.chunk_count)
print('checksum:', d.checksum[:16], '| nome:', d.original_filename)
print('fingerprint == KB:', d.index_fingerprint == kb.index_fingerprint())
print('disallineato:', d.needs_reindex)
for c in d.chunks.all():
    print(' segmento', c.ordinal, 'pagina', c.page_number, 'vector_id', c.vector_id, 'caratteri', c.char_count)
assert d.status == 'indexed' and d.chunk_count == d.chunks.count()
print('DOC_ID', d.pk)
"

# 2. I vettori sono nella collezione della KB, con gli id deterministici
.venv/Scripts/python.exe manage.py shell -c "
from django.db import connection
with connection.cursor() as c:
    c.execute('''select e.id, e.cmetadata->>'page', e.cmetadata->>'ordinal', length(e.document)
                 from langchain_pg_embedding e join langchain_pg_collection k
                 on k.uuid = e.collection_id where k.name = 'default' order by e.id''')
    for r in c.fetchall(): print(r)
"

# 3. Idempotenza: reindicizzare non duplica (decisione 1)
.venv/Scripts/python.exe manage.py shell -c "
from django.db import connection
from rag.models import Document
from rag.services.ingestion import ingest_document
d = Document.objects.filter(status='indexed').first()
def conta():
    with connection.cursor() as c:
        c.execute(\"select count(*) from langchain_pg_embedding e join langchain_pg_collection k on k.uuid=e.collection_id where k.name='default'\")
        return c.fetchone()[0]
prima_v, prima_c = conta(), d.chunks.count()
pk_prima = list(d.chunks.values_list('pk', flat=True))
print('esito:', ingest_document(d))
print('vettori', prima_v, '->', conta(), '| chunk', prima_c, '->', d.chunks.count())
print('chiavi primarie dei chunk conservate:', pk_prima == list(d.chunks.values_list('pk', flat=True)))
assert conta() == prima_v and d.chunks.count() == prima_c
"

# 4. Deduplica (RF-09): lo stesso file nella stessa KB viene rifiutato
.venv/Scripts/python.exe manage.py shell -c "
from django.core.files import File
from rag.models import Document, RagPipeline
from rag.services.ingestion import ingest_document
from rag.services.exceptions import DocumentoDuplicato
kb = RagPipeline.objects.get(is_default=True).knowledge_base
d2 = Document(knowledge_base=kb)
with open('samples/manuale-dipendenti.pdf','rb') as f:
    d2.file.save('duplicato.pdf', File(f), save=False)
d2.save()
try:
    ingest_document(d2); print('FALLITO: duplicato non rilevato')
except DocumentoDuplicato as exc:
    d2.refresh_from_db()
    print('OK ->', exc)
    print('stato del duplicato:', d2.status, '| messaggio persistito:', bool(d2.error_message))
d2.file.delete(save=False); d2.delete()
"

# 5. Reindicizzazione con meno chunk: gli ordinali in eccesso spariscono da entrambi i lati
# CORRETTO IN ESECUZIONE: il piano prescriveva 200/20 poi 4000/200, ma il
# conteggio resta 3 in tutti e tre i casi — split_documents() non fonde mai
# pagine diverse, quindi il numero di chunk non puo' scendere sotto il numero di
# pagine con testo, e una finestra di 200 contiene gia' intere le pagine da
# 166/147/126 caratteri. Con 4000/200 non c'era alcun ordinale in eccesso: il
# ramo store.delete(ids=...) — cioe' cio' che la verifica esiste per provare —
# non veniva MAI eseguito e l'assert passava a vuoto. Corretto a 100/10 (6
# chunk) -> 800/120 (3 chunk), controllando gli ID dei vettori e vettori_rimossi.
.venv/Scripts/python.exe manage.py shell -c "
from django.db import connection
from rag.models import Document
from rag.services.ingestion import ingest_document
d = Document.objects.filter(status='indexed').first()
kb = d.knowledge_base; p = kb.chunking_profile
originali = (p.chunk_size, p.chunk_overlap)
def ids():
    with connection.cursor() as c:
        c.execute(\"select e.id from langchain_pg_embedding e join langchain_pg_collection k on k.uuid=e.collection_id where k.name='default' order by e.id\")
        return [r[0] for r in c.fetchall()]
p.chunk_size, p.chunk_overlap = 100, 10; p.save()   # piu' chunk
e1 = ingest_document(d); d.refresh_from_db()
print('a 100/10:', d.chunk_count, 'segmenti | rimossi', e1.vettori_rimossi, '| vettori', ids())
assert len(ids()) == d.chunk_count == d.chunks.count()
p.chunk_size, p.chunk_overlap = originali; p.save()  # meno chunk: pulizia attesa
e2 = ingest_document(d); d.refresh_from_db()
print('a', originali, ':', d.chunk_count, 'segmenti | rimossi', e2.vettori_rimossi, '| vettori', ids())
print('ordinali chunk:', list(d.chunks.values_list('ordinal', flat=True)))
assert len(ids()) == d.chunk_count == d.chunks.count() == 3, 'vettori orfani non rimossi'
assert e2.vettori_rimossi == 3
print('OK entrambe le direzioni')
"

# 6. CA-8: PDF corrotto -> stato «Fallito» con motivo leggibile, sistema integro
.venv/Scripts/python.exe manage.py shell -c "
from django.core.files.base import ContentFile
from rag.models import Document, RagPipeline
from rag.services.ingestion import ingest_document
from rag.services.exceptions import IngestionError
kb = RagPipeline.objects.get(is_default=True).knowledge_base
d = Document(knowledge_base=kb)
d.file.save('corrotto.pdf', ContentFile(b'non sono un pdf'), save=False)
d.save()
try:
    ingest_document(d)
except IngestionError as exc:
    d.refresh_from_db()
    print('stato:', d.status); print('messaggio:', d.error_message)
    assert d.status == 'failed' and d.error_message
d.file.delete(save=False); d.delete()
print('OK')
"

# 7. AGGIUNTA IN ESECUZIONE: batch_size e' davvero onorato (decisione 4).
# Il profilo predefinito ha batch_size=16 e il PDF di prova produce 3 segmenti:
# con quei valori l'affettamento non viene mai esercitato e la decisione 4
# resterebbe indistinguibile da un campo decorativo. Abbassarlo a 2 deve
# produrre DUE POST a /api/embed invece di una (oltre alla sonda).
.venv/Scripts/python.exe manage.py shell -c "
import logging
logging.getLogger('rag.services.ingestion').setLevel(logging.DEBUG)
from rag.models import Document
from rag.services.ingestion import ingest_document
d = Document.objects.filter(status='indexed').first()
p = d.knowledge_base.embedding_profile
originale = p.batch_size
p.batch_size = 2; p.save()
print('con batch_size=2:', ingest_document(d))
p.batch_size = originale; p.save()
d.refresh_from_db()
print('ripristinato batch_size a', originale, '| stato', d.status, '| chunk', d.chunk_count, d.chunks.count())
"
```
Il documento creato al punto 1 **va lasciato nel database**: serve alla fase 4 e
alla verifica di fase. Annotare il suo id nel report.

**Phase Complete When:**
- [x] `rag/services/ingestion.py` esiste con `vector_id`, `compute_checksum`,
      `trova_duplicato`, `ingest_document`, `EsitoIngestione`
- [x] Le sei verifiche passano, **incluse quelle di idempotenza (3), deduplica (4),
      rimozione degli obsoleti (5) e CA-8 (6)**
- [x] Un documento in stato `indexed` esiste, con `chunk_count == chunks.count() ==`
      numero di righe nella collezione `default`
- [x] `page_count` riporta il totale delle pagine del file, non delle pagine con testo
- [x] `index_fingerprint` del documento coincide con quello della KB → `needs_reindex` è falso
- [x] `manage.py makemigrations --check --dry-run` → «No changes detected»
- [x] Commit: `P2: servizio di ingestione, macchina a stati e deduplica (T-17, T-20)`
- [x] Sezione del report scritta, **con l'id del documento indicizzato**

---

### Fase 4: Comando di gestione e innesco dall'admin

**Status:** DONE

**Attività di backlog coperte:** T-18, T-19

**Stima:** 1 h 30

**Read first:**
- `REQUIREMENTS.md` RF-07, RF-25, RF-28, RF-29 e CA-2
- `rag/admin.py` — `DocumentAdmin` per intero: `readonly_fields`, i fieldset e la
  colonna `disallineato` esistono già, e le descrizioni dicono «a partire da P2»
- `plans/2026-07-24-1834-P1-plan-report.md`, «Debiti aperti verso P2» — la prima
  voce spiega **perché** il form non valida il checksum
- `rag/services/ingestion.py`, appena scritto
- Di questo piano: le decisioni **5, 7, 8**

**Files to modify:**
- `rag/management/__init__.py`, `rag/management/commands/__init__.py` (nuovi, vuoti)
- `rag/management/commands/ingest.py` (nuovo)
- `rag/admin.py` (modificato)

**Changes:**

- [x] **4.1** Creare l'alberatura dei comandi:
  ```bash
  mkdir -p rag/management/commands
  touch rag/management/__init__.py rag/management/commands/__init__.py
  ```

- [x] **4.2** Creare `rag/management/commands/ingest.py` (T-18):
  ```python
  """Ingestione da riga di comando (T-18, RF-28).

  Serve a due cose che l'admin non copre: automazione, e prova senza browser
  durante lo sviluppo. Il percorso passato non viene indicizzato «in loco»: il
  file viene COPIATO in MEDIA_ROOT come qualunque altro caricamento, perche' un
  Document deve possedere il proprio file (l'admin ne offre il download, CA-2).
  """

  from pathlib import Path

  from django.core.files import File
  from django.core.management.base import BaseCommand, CommandError

  from rag.models import Document, KnowledgeBase, RagPipeline
  from rag.services.exceptions import IngestionError
  from rag.services.ingestion import compute_checksum, ingest_document, trova_duplicato


  class Command(BaseCommand):
      help = "Carica un PDF in una base di conoscenza e lo indicizza."

      def add_arguments(self, parser):
          # nargs="?" perche' con --reindex il file e' gia' quello del documento:
          # obbligare a ripeterne il percorso sarebbe un argomento ignorato.
          parser.add_argument(
              "path", nargs="?", default=None, help="Percorso del file PDF da indicizzare."
          )
          parser.add_argument(
              "--kb",
              dest="kb",
              default=None,
              help=(
                  "Nome o id della base di conoscenza. Se omesso, si usa quella "
                  "della pipeline predefinita (RF-26)."
              ),
          )
          parser.add_argument(
              "--reindex",
              dest="reindex",
              type=int,
              default=None,
              metavar="ID",
              help="Reindicizza un documento esistente invece di caricarne uno nuovo.",
          )

      def handle(self, *args, **options):
          if options["reindex"] is not None:
              documento = self._documento_esistente(options["reindex"])
          elif options["path"] is None:
              raise CommandError(
                  "Indicare il percorso di un PDF, oppure --reindex ID per "
                  "reindicizzare un documento gia' caricato."
              )
          else:
              documento = self._crea_documento(options["path"], options["kb"])

          self.stdout.write(
              f"Indicizzazione del documento {documento.pk} "
              f"nella base «{documento.knowledge_base.name}»…"
          )
          self.stdout.write(
              self.style.WARNING(
                  "L'ingestione e' sincrona in P2 e il primo embedding puo' "
                  "richiedere fino a ~20 s per il caricamento del modello in VRAM."
              )
          )
          try:
              esito = ingest_document(documento)
          except IngestionError as exc:
              # Lo stato «Fallito» e il motivo sono gia' persistiti sul
              # documento: al comando resta da riportarlo in modo leggibile.
              raise CommandError(str(exc)) from exc

          self.stdout.write(self.style.SUCCESS(f"Fatto — {esito}"))

      def _documento_esistente(self, pk: int) -> Document:
          documento = Document.objects.filter(pk=pk).first()
          if documento is None:
              raise CommandError(f"Nessun documento con id {pk}.")
          return documento

      def _crea_documento(self, path: str, kb_arg: str | None) -> Document:
          percorso = Path(path)
          if not percorso.is_file():
              raise CommandError(f"File non trovato: {percorso}")

          kb = self._base_di_conoscenza(kb_arg)

          # La deduplica si verifica PRIMA di copiare il file in MEDIA_ROOT:
          # rifiutare dopo la copia lascerebbe un file orfano sul disco.
          with percorso.open("rb") as f:
              checksum = compute_checksum(File(f))
          esistente = trova_duplicato(kb.pk, checksum)
          if esistente is not None:
              raise CommandError(
                  # CORRETTO IN ESECUZIONE: il piano suggeriva anche il
                  # percorso dopo --reindex, ma con --reindex il percorso viene
                  # IGNORATO (il file e' gia' quello del documento): suggerirlo
                  # insegnerebbe un uso sbagliato.
                  f"Questo file e' gia' presente nella base «{kb.name}» come "
                  f"documento {esistente.pk}. Per rifarne l'indice: "
                  f"manage.py ingest --reindex {esistente.pk}"
              )

          documento = Document(knowledge_base=kb, original_filename=percorso.name)
          with percorso.open("rb") as f:
              documento.file.save(percorso.name, File(f), save=False)
          documento.save()
          return documento

      def _base_di_conoscenza(self, kb_arg: str | None) -> KnowledgeBase:
          if kb_arg is None:
              pipeline = RagPipeline.objects.filter(is_default=True).first()
              if pipeline is None:
                  raise CommandError(
                      "Nessuna pipeline predefinita: indicare la base di "
                      "conoscenza con --kb."
                  )
              return pipeline.knowledge_base
          if kb_arg.isdigit():
              kb = KnowledgeBase.objects.filter(pk=int(kb_arg)).first()
          else:
              kb = KnowledgeBase.objects.filter(name=kb_arg).first()
          if kb is None:
              disponibili = ", ".join(KnowledgeBase.objects.values_list("name", flat=True))
              raise CommandError(f"Base di conoscenza «{kb_arg}» inesistente. Disponibili: {disponibili}")
          return kb
  ```

- [x] **4.3** In `rag/admin.py`, aggiungere gli import necessari accanto a quelli
  esistenti:
  ```python
  from pathlib import Path

  from django import forms
  from django.contrib import admin, messages
  from django.core.files.uploadedfile import UploadedFile
  from django.db.models import Count

  from .services.exceptions import IngestionError
  from .services.ingestion import compute_checksum, ingest_document, trova_duplicato
  ```

- [x] **4.4** In `rag/admin.py`, aggiungere il form del documento **prima** di
  `DocumentAdmin`. È la risposta al primo debito lasciato da P1:
  ```python
  class DocumentAdminForm(forms.ModelForm):
      """Deduplica al momento del caricamento (RF-09).

      Perche' serve un form invece del solo vincolo di database: il ModelForm
      dell'admin esclude dalla validazione i campi in readonly_fields, e
      `checksum` e' fra quelli. Il vincolo document_checksum_unico_per_kb non
      viene quindi mai verificato dalla pagina, e un duplicato arriverebbe al
      database come IntegrityError, cioe' una 500. Accertato in P1, passo 4.5.

      Il controllo qui evita anche di CREARE la riga: l'alternativa —
      intercettare in ingest_document e marcare «Fallito» — lascerebbe in elenco
      un documento inutile per ogni tentativo.
      """

      class Meta:
          model = Document
          fields = "__all__"

      def clean(self):
          dati = super().clean()
          file = dati.get("file")
          kb = dati.get("knowledge_base")
          if not file or not kb:
              return dati

          # Solo sui file appena CARICATI. Su una modifica che non tocca il file,
          # cleaned_data["file"] e' il FieldFile gia' salvato: ricalcolarne il
          # checksum vorrebbe dire rileggere dal disco per confrontarlo con se
          # stesso, e su Windows lascerebbe un handle aperto.
          # isinstance(..., UploadedFile) e' il discriminante corretto: verificato
          # che un FieldFile NON e' un UploadedFile, mentre un upload lo e'.
          if not isinstance(file, UploadedFile):
              return dati

          checksum = compute_checksum(file)   # seek(0) incluso: senza, si
                                              # salverebbe un file vuoto
          esistente = trova_duplicato(kb.pk, checksum, escludi_id=self.instance.pk)
          if esistente is not None:
              raise forms.ValidationError(
                  {
                      "file": (
                          f"Questo file e' gia' presente nella base di conoscenza "
                          f"«{kb.name}» come documento {esistente.pk} "
                          f"({esistente}). Per rifarne l'indice usare l'azione "
                          "«Reindicizza i documenti selezionati»."
                      )
                  }
              )
          return dati
  ```

- [x] **4.5** In `DocumentAdmin`, agganciare il form, l'innesco e l'azione di
  reindicizzazione. `save_model` e non un signal `post_save`, per la decisione 8:
  ```python
      form = DocumentAdminForm
      actions = ["reindicizza"]

      def save_model(self, request, obj, form, change):
          """Salva e indicizza (RF-29).

          L'innesco sta qui e non in un signal post_save: il servizio salva il
          documento piu' volte — «In elaborazione», poi lo stato finale — e un
          post_save si richiamerebbe da se'. In P5 (T-32) questa chiamata
          diventa un enqueue e resta l'unico punto da modificare.
          """
          if not obj.original_filename and obj.file:
              obj.original_filename = Path(obj.file.name).name
          super().save_model(request, obj, form, change)

          # Un documento gia' indicizzato che viene salvato senza cambiare file
          # non va reindicizzato di nascosto: per quello c'e' l'azione.
          if change and obj.status == Document.Status.INDEXED and "file" not in form.changed_data:
              return

          try:
              esito = ingest_document(obj)
          except IngestionError as exc:
              self.message_user(request, f"Indicizzazione non riuscita: {exc}", messages.ERROR)
          except Exception as exc:  # noqa: BLE001 - l'admin non deve mostrare una 500
              self.message_user(
                  request,
                  f"Indicizzazione interrotta da un errore inatteso: {exc}. "
                  "Il motivo e' registrato sul documento.",
                  messages.ERROR,
              )
          else:
              self.message_user(
                  request,
                  f"Documento indicizzato: {esito.chunk_count} segmenti su "
                  f"{esito.page_count} pagine in {esito.durata_s:.1f}s.",
                  messages.SUCCESS,
              )

      @admin.action(description="Reindicizza i documenti selezionati")
      def reindicizza(self, request, queryset):
          """RF-07 e RF-25: rielaborare senza ricaricare.

          Sincrona, quindi con N documenti la richiesta dura N ingestioni. E' il
          limite dichiarato di P2, che T-32 rimuove.
          """
          riusciti = falliti = 0
          for documento in queryset:
              try:
                  ingest_document(documento)
                  riusciti += 1
              except Exception as exc:  # noqa: BLE001 - un fallimento non ferma gli altri
                  falliti += 1
                  self.message_user(
                      request, f"{documento}: {exc}", messages.ERROR
                  )
          if riusciti:
              self.message_user(
                  request, f"{riusciti} documenti reindicizzati.", messages.SUCCESS
              )
          if falliti:
              self.message_user(
                  request,
                  f"{falliti} documenti non reindicizzati: il motivo e' sul singolo documento.",
                  messages.WARNING,
              )
  ```
  Aggiungere `from pathlib import Path` in testa al file.

- [x] **4.6** In `EmbeddingProfileAdmin`, completare la descrizione del fieldset
  «Parametri» perché `normalize` e `batch_size` dicano la verità (decisione 5):
  ```python
      # sostituisce: ("Parametri", {"fields": ("dimension", "normalize", "batch_size")}),
      (
          "Parametri",
          {
              "fields": ("dimension", "normalize", "batch_size"),
              "description": (
                  "«Dimensione» e' verificata a ogni ingestione: se il modello "
                  "produce vettori di lunghezza diversa, l'indicizzazione si "
                  "ferma con un errore invece di corrompere l'indice in silenzio. "
                  "«Dimensione del lotto» e' il numero di segmenti per chiamata di "
                  "embedding, rispettato dal servizio di ingestione. "
                  "«Normalizza» e' onorato dal modello stesso — bge-m3 restituisce "
                  "vettori a norma 1 — e resta comunque inerte con la distanza "
                  "cosine usata da pgvector, che e' invariante alla scala: il "
                  "codice non lo applica, e il campo lo dichiara."
              ),
          },
      ),
  ```

- [x] **4.7** Aggiornare le due descrizioni di `DocumentAdmin` che parlano al
  futuro, ora che il futuro è arrivato:
  - nel primo fieldset, sostituire «Il caricamento avvia l'ingestione a partire da
    P2 (T-17). In questa fase il documento resta «In attesa».» con:
    «Il salvataggio avvia immediatamente l'indicizzazione, che è **sincrona**: la
    pagina resta in attesa per tutta la sua durata, e il primo embedding dopo
    l'avvio può richiedere ~20 s per il caricamento del modello in VRAM. Lo stesso
    file non può essere caricato due volte nella stessa base di conoscenza.»
  - nel fieldset «Indicizzazione», aggiungere in coda alla descrizione: «Per
    riallineare un documento, usare l'azione «Reindicizza i documenti
    selezionati».»
  - nella docstring in testa a `rag/admin.py`, sostituire la «Nota di fase» che
    dice che l'azione «reindicizza» è ancora da fare.

**Verify:**
```bash
# 1. Il comando funziona ed è idempotente sul rifiuto del duplicato (T-18, RF-09)
.venv/Scripts/python.exe manage.py ingest samples/manuale-dipendenti.pdf
# atteso: CommandError, il file e' gia' presente (caricato in fase 3)

# 2. Il comando su una KB nuova funziona (stesso file, KB diversa: e' legittimo)
.venv/Scripts/python.exe manage.py shell -c "
from rag.models import ChunkingProfile, EmbeddingProfile, KnowledgeBase
kb, creata = KnowledgeBase.objects.get_or_create(
    name='Prova P2', defaults={'collection_name': 'prova_p2',
    'embedding_profile': EmbeddingProfile.objects.first(),
    'chunking_profile': ChunkingProfile.objects.first()})
print('KB', kb.pk, 'creata' if creata else 'esistente')
"
.venv/Scripts/python.exe manage.py ingest samples/manuale-dipendenti.pdf --kb "Prova P2"
# atteso: «Fatto — documento N: 3 segmenti su 3 pagine in …s»

# 3. Il reindex da comando (RF-07)
.venv/Scripts/python.exe manage.py shell -c "
from rag.models import Document
print(Document.objects.filter(status='indexed').values_list('pk','chunk_count'))
"
.venv/Scripts/python.exe manage.py ingest --reindex <ID>
# nessun percorso: con --reindex il file e' gia' quello del documento

# 4. Errori del comando, tutti leggibili
.venv/Scripts/python.exe manage.py ingest samples/inesistente.pdf          # File non trovato
.venv/Scripts/python.exe manage.py ingest samples/manuale-dipendenti.pdf --kb "Inesistente"

# 5. L'upload dall'admin innesca l'ingestione (RF-29, CA-2) — via test client
#
# ATTENZIONE — i quattro campi «chunks-*» NON sono opzionali. Senza di essi
# l'admin risponde 200 con «ManagementForm … Campi mancanti: chunks-TOTAL_FORMS,
# chunks-INITIAL_FORMS» e NON crea nulla: la verifica sembrerebbe superata (zero
# documenti creati!) mentre in realta' non ha mai raggiunto il form. Verificato
# in fase di pianificazione. «chunks» e' il prefisso dell'inline, dedotto dal
# related_name.
.venv/Scripts/python.exe manage.py shell -c "
from django.contrib.auth import get_user_model
from django.test import Client
from rag.models import Document, RagPipeline
INLINE = {'chunks-TOTAL_FORMS': '0', 'chunks-INITIAL_FORMS': '0',
          'chunks-MIN_NUM_FORMS': '0', 'chunks-MAX_NUM_FORMS': '0'}
U = get_user_model()
U.objects.filter(username='verifica_p2').delete()
U.objects.create_superuser('verifica_p2', 'v@example.com', 'verifica-p2-2026')
c = Client(); assert c.login(username='verifica_p2', password='verifica-p2-2026')
kb = RagPipeline.objects.get(is_default=True).knowledge_base
prima = Document.objects.count()
with open('samples/manuale-dipendenti.pdf','rb') as f:
    r = c.post('/admin/rag/document/add/',
               {'knowledge_base': kb.pk, 'original_filename': '', 'file': f, **INLINE},
               follow=True)
print('status', r.status_code)
testo = r.content.decode('utf8', 'ignore')
assert 'ManagementForm' not in testo, 'il POST non ha raggiunto il form: campi chunks-* mancanti'
print('errore sul campo file presente:', 'gia&#x27; presente' in testo or 'gia\' presente' in testo)
print('documenti', prima, '->', Document.objects.count())
assert Document.objects.count() == prima, 'il duplicato ha creato una riga'
"
# atteso: il duplicato viene RIFIUTATO dal form, nessun documento creato,
# errore sul campo «file». E' la verifica di RF-09 dall'admin.

# 6. Upload di un PDF nuovo dall'admin -> indicizzato con messaggio di successo
.venv/Scripts/python.exe manage.py shell -c "
import pymupdf, tempfile, os
from django.test import Client
# CORRETTO IN ESECUZIONE: senza setup_test_environment() il segnale
# template_rendered non e' attivo e response.context resta None, quindi
# r.context['messages'] solleva TypeError DOPO che l'ingestione e' gia'
# avvenuta. Vale per la verifica 6 e per la 7.
from django.test.utils import setup_test_environment
setup_test_environment()
from rag.models import Document, RagPipeline
p = os.path.join(tempfile.gettempdir(), 'nuovo.pdf')
d = pymupdf.open(); pg = d.new_page()
pg.insert_text((72, 100), 'Il rimborso pasto giornaliero e di 12 euro.')
pg2 = d.new_page(); pg2.insert_text((72, 100), 'Il preavviso di dimissioni e di 30 giorni.')
d.save(p); d.close()
INLINE = {'chunks-TOTAL_FORMS': '0', 'chunks-INITIAL_FORMS': '0',
          'chunks-MIN_NUM_FORMS': '0', 'chunks-MAX_NUM_FORMS': '0'}
c = Client(); c.login(username='verifica_p2', password='verifica-p2-2026')
kb = RagPipeline.objects.get(is_default=True).knowledge_base
with open(p,'rb') as f:
    r = c.post('/admin/rag/document/add/',
               {'knowledge_base': kb.pk, 'original_filename': '', 'file': f, **INLINE},
               follow=True)
for m in r.context['messages']: print('MESSAGGIO:', m.level_tag, m.message)
nuovo = Document.objects.order_by('-pk').first()
print('stato:', nuovo.status, '| pagine:', nuovo.page_count, '| segmenti:', nuovo.chunk_count, '| nome:', nuovo.original_filename)
# il PDF deve essere stato scritto per intero: se compute_checksum non riavvolge
# il puntatore, qui si troverebbero 0 byte (cfr. passo 3.2)
print('byte del file salvato:', nuovo.file.size)
assert nuovo.file.size > 0 and nuovo.status == 'indexed'
os.remove(p)
"

# 7. L'azione «reindicizza» compare ed è eseguibile
.venv/Scripts/python.exe manage.py shell -c "
from django.test import Client
from django.test.utils import setup_test_environment   # CORRETTO IN ESECUZIONE, cfr. verifica 6
setup_test_environment()
from rag.models import Document
c = Client(); c.login(username='verifica_p2', password='verifica-p2-2026')
ids = list(Document.objects.filter(status='indexed').values_list('pk', flat=True))
r = c.post('/admin/rag/document/', {'action': 'reindicizza', '_selected_action': ids}, follow=True)
for m in r.context['messages']: print('MESSAGGIO:', m.level_tag, m.message)
"

# 8. RF-25 dall'admin: cambiare il profilo di segmentazione segnala il disallineamento
.venv/Scripts/python.exe manage.py shell -c "
from rag.models import Document, ChunkingProfile
d = Document.objects.filter(status='indexed').first()
p = d.knowledge_base.chunking_profile
print('prima:', d.needs_reindex)
originali = (p.chunk_size, p.chunk_overlap)
p.chunk_size = 1000; p.save()
d.refresh_from_db(); print('dopo la modifica del profilo:', d.needs_reindex)
assert d.needs_reindex, 'il disallineamento non viene rilevato'
p.chunk_size, p.chunk_overlap = originali; p.save()
d.refresh_from_db(); print('ripristinato:', d.needs_reindex)
"
```

**Phase Complete When:**
- [x] `manage.py ingest <path>` carica e indicizza; `--kb` e `--reindex` funzionano;
      gli errori sono `CommandError` leggibili e non stack trace
- [x] Un upload dall'admin porta il documento a *indicizzato* senza altri passi (RF-29, CA-2)
- [x] Il caricamento di un duplicato è **rifiutato dal form**, con errore sul campo
      `file` e **senza creare la riga** (RF-09)
- [x] L'azione «Reindicizza i documenti selezionati» esiste e funziona su una selezione multipla (RF-07)
- [x] La colonna «disallineato» diventa vera cambiando un profilo d'indice e falsa dopo la reindicizzazione (RF-25)
- [x] Le descrizioni dell'admin non parlano più di P2 al futuro, e `normalize`
      dichiara di essere inerte (decisione 5)
- [x] `manage.py makemigrations --check --dry-run` → «No changes detected»
      (la modifica al fieldset dell'admin **non** è una modifica al modello)
- [x] Commit: `P2: comando ingest, innesco dall'admin e azione di reindicizzazione (T-18, T-19)`
- [x] Sezione del report scritta

---

### Fase 5: Cancellazione dei vettori, verifica di fase e pulizia

**Status:** DONE

**Attività di backlog coperte:** T-20 (parte relativa a RF-08), verifica di fase

**Stima:** 1 h 15

**Read first:**
- `ARCHITECTURE.md` §6.3 (Django non conosce le tabelle `langchain_pg_*`) e §6.4
  (la riga `DocumentChunk → Document`: «`CASCADE` + hook che rimuove i vettori»)
- `REQUIREMENTS.md` RF-08 e CA-2, CA-8
- `rag/apps.py` — è ancora quello generato da `startapp`, senza `ready()`
- `plans/2026-07-24-1834-P1-plan-report.md`, «Debiti aperti verso P2», seconda e
  quarta voce
- Di questo piano: la decisione **9** e la decisione **11** (lo spike sopravvive,
  la collezione no)

**Files to modify:**
- `rag/signals.py` (nuovo)
- `rag/apps.py` (modificato)

**Changes:**

- [x] **5.1** Creare `rag/signals.py`. I tre meccanismi combinati sono la
  decisione 9, e ognuno c'è per un motivo diverso:
  ```python
  """Rimozione dei vettori alla cancellazione di un documento (RF-08, T-20).

  Serve un hook perche' i due lati dello schema hanno proprietari diversi
  (ARCHITECTURE §6.3): il CASCADE di Django elimina le righe DocumentChunk, ma
  langchain_pg_embedding non e' una tabella che Django conosca, quindi i vettori
  sopravvivrebbero al documento — e resterebbero recuperabili dal retrieval di
  P3, che risponderebbe citando un documento cancellato.

  Perche' TRE meccanismi e non uno:

  - `pre_delete` raccoglie i vector_id FINCHE' le righe DocumentChunk esistono:
    il collector di Django cancella i figli prima del padre, quindi in
    `post_delete` sarebbero gia' spariti;
  - `post_delete` programma la pulizia solo dopo che la cancellazione e' andata
    a buon fine;
  - `transaction.on_commit()` la rinvia al commit, perche' pgvector sta su
    un'altra connessione: eseguirla subito e vedere poi un rollback di Django
    lascerebbe un documento vivo SENZA vettori, che e' la direzione sbagliata del
    guasto. Al contrario, dei vettori orfani sono innocui e ricalcolabili, perche'
    gli id sono deterministici.

  In caso di errore si registra nel log e NON si risolleva: il documento e' gia'
  cancellato, e una 500 dopo un'operazione riuscita sarebbe una bugia.
  """

  import logging

  from django.db import transaction
  from django.db.models.signals import post_delete, pre_delete
  from django.dispatch import receiver

  from .models import Document

  logger = logging.getLogger(__name__)


  @receiver(pre_delete, sender=Document, dispatch_uid="rag.documento.raccogli_vettori")
  def raccogli_vettori(sender, instance: Document, **kwargs) -> None:
      """Fotografa cio' che servira' dopo la cancellazione.

      Anche la KnowledgeBase va letta ora: se la cancellazione arriva in cascata
      dalla KB, in post_delete quella riga potrebbe non esserci piu'.
      """
      instance._vettori_da_rimuovere = list(
          instance.chunks.exclude(vector_id="").values_list("vector_id", flat=True)
      )
      instance._kb_per_pulizia = instance.knowledge_base


  @receiver(post_delete, sender=Document, dispatch_uid="rag.documento.rimuovi_vettori")
  def rimuovi_vettori(sender, instance: Document, **kwargs) -> None:
      ids = getattr(instance, "_vettori_da_rimuovere", None)
      kb = getattr(instance, "_kb_per_pulizia", None)
      if not ids or kb is None:
          return

      document_pk = instance.pk

      def pulisci() -> None:
          # Import locale: importare le factory a livello di modulo tirerebbe
          # dentro LangChain al caricamento delle app.
          from .services.factories import get_vectorstore

          try:
              get_vectorstore(kb).delete(ids=list(ids))
              logger.info(
                  "Rimossi %s vettori del documento %s dalla collezione «%s».",
                  len(ids), document_pk, kb.collection_name,
              )
          except Exception:  # noqa: BLE001 - il documento e' gia' cancellato
              logger.exception(
                  "Vettori del documento %s non rimossi dalla collezione «%s»: "
                  "restano orfani, ricalcolabili perche' gli id sono "
                  "deterministici (%s…).",
                  document_pk, kb.collection_name, ids[0],
              )

      transaction.on_commit(pulisci)
  ```

- [x] **5.2** Registrare i signal in `rag/apps.py`. `ready()` è l'unico posto
  corretto: importarli da `models.py` li caricherebbe a metà
  dell'inizializzazione delle app.
  ```python
  from django.apps import AppConfig


  class RagConfig(AppConfig):
      name = "rag"
      verbose_name = "Sistema RAG"

      def ready(self) -> None:
          # L'import ha come solo scopo la registrazione dei @receiver.
          from . import signals  # noqa: F401
  ```

- [x] **5.3** Rimuovere la collezione `spike` lasciata da P0 (decisione 11).
  **Non** cancellare `scripts/spike_rag.py`: resta l'unica dimostrazione
  funzionante che il sistema risponde, finché `manage.py ask` non esiste in P3.
  Lo script ricrea la collezione da sé (`pre_delete_collection=True`), quindi
  rimuoverla ora non lo rompe.
  ```bash
  .venv/Scripts/python.exe manage.py shell -c "
  from django.db import connection
  from langchain_postgres import PGVector
  from rag.models import EmbeddingProfile
  from rag.services.factories import connection_string, get_embeddings
  store = PGVector(
      embeddings=get_embeddings(EmbeddingProfile.objects.first()),
      collection_name='spike',
      connection=connection_string(),
      use_jsonb=True,
      create_extension=False,
  )
  store.delete_collection()
  with connection.cursor() as c:
      c.execute('select name from langchain_pg_collection order by name')
      print('collezioni residue:', [r[0] for r in c.fetchall()])
  "
  ```

- [x] **5.4** Ripulire i dati di prova accumulati dalle verifiche delle fasi 3 e 4:
  la base di conoscenza `Prova P2`, i documenti di prova diversi da quello del
  manuale, e l'utente `verifica_p2`. La cancellazione della KB è anche la
  **verifica di RF-08 in cascata**: i vettori dei suoi documenti devono sparire.
  ```bash
  .venv/Scripts/python.exe manage.py shell -c "
  from django.contrib.auth import get_user_model
  from django.db import connection
  from rag.models import Document, KnowledgeBase
  def vettori(nome):
      with connection.cursor() as c:
          c.execute(\"select count(*) from langchain_pg_embedding e join langchain_pg_collection k on k.uuid=e.collection_id where k.name=%s\", [nome])
          return c.fetchone()[0]
  print('vettori in prova_p2, prima:', vettori('prova_p2'))
  kb = KnowledgeBase.objects.filter(name='Prova P2').first()
  if kb: kb.delete()
  print('vettori in prova_p2, dopo la cancellazione della KB:', vettori('prova_p2'))
  # Resta un solo documento: il manuale nella KB predefinita
  for d in Document.objects.exclude(original_filename='manuale-dipendenti.pdf'):
      print('rimuovo', d.pk, d); d.file.close(); d.file.delete(save=False); d.delete()
  get_user_model().objects.filter(username='verifica_p2').delete()
  print('documenti residui:', list(Document.objects.values_list('pk','original_filename','status')))
  print('vettori in default:', vettori('default'))
  "
  ```

**Verify:**
```bash
# 1. RF-08 sul singolo documento: cancellarlo rimuove chunk E vettori
.venv/Scripts/python.exe manage.py shell -c "
import pymupdf, tempfile, os
from django.core.files import File
from django.db import connection
from rag.models import Document, RagPipeline
from rag.services.ingestion import ingest_document
def vettori():
    with connection.cursor() as c:
        c.execute(\"select count(*) from langchain_pg_embedding e join langchain_pg_collection k on k.uuid=e.collection_id where k.name='default'\")
        return c.fetchone()[0]
p = os.path.join(tempfile.gettempdir(), 'da-cancellare.pdf')
d0 = pymupdf.open(); pg = d0.new_page()
pg.insert_text((72, 100), 'Documento creato solo per verificare RF-08.')
d0.save(p); d0.close()
kb = RagPipeline.objects.get(is_default=True).knowledge_base
prima = vettori()
doc = Document(knowledge_base=kb)
with open(p,'rb') as f: doc.file.save('da-cancellare.pdf', File(f), save=False)
doc.save(); ingest_document(doc); doc.refresh_from_db()
print('vettori:', prima, '->', vettori(), '| chunk:', doc.chunks.count())
ids = list(doc.chunks.values_list('vector_id', flat=True)); print('id dei vettori:', ids)
doc.file.delete(save=False); doc.delete()
print('dopo la cancellazione — vettori:', vettori(), '(atteso', prima, ')')
assert vettori() == prima, 'RF-08 non rispettato: vettori orfani'
os.remove(p); print('OK')
"

# 2. Verifica di fase completa (BACKLOG): upload dall'admin -> indicizzato,
#    chunk visibili con pagina e ordinale. Da fare A MANO nel browser.
.venv/Scripts/python.exe manage.py runserver
#   a) http://localhost:8000/admin/ — accedere con un superuser
#   b) Documenti -> Aggiungi: scegliere la KB predefinita e un PDF con testo
#   c) Salva: attendere. Atteso un messaggio verde «Documento indicizzato: N
#      segmenti su M pagine in …s»
#   d) In elenco: stato «Indicizzato», pagine e segmenti valorizzati,
#      «disallineato» falso  → CA-2
#   e) Aprire il documento: l'inline dei segmenti mostra ordinale, pagina,
#      caratteri, estratto e vector_id  → verifica di fase
#   f) Ricaricare lo STESSO file: errore sul campo «file», nessuna riga creata → RF-09
#   g) Caricare un file non-PDF rinominato .pdf: stato «Fallito» con motivo
#      leggibile, admin integro → CA-8
#   h) Modificare il profilo di segmentazione, tornare all'elenco: «disallineato»
#      vero; azione «Reindicizza i documenti selezionati»; torna falso → RF-25
#   i) Cancellare un documento dall'elenco e ricontare i vettori → RF-08

# 3. Coerenza finale fra le due metà dello schema
.venv/Scripts/python.exe manage.py shell -c "
from django.db import connection
from rag.models import Document, DocumentChunk
with connection.cursor() as c:
    c.execute('''select k.name, count(e.id) from langchain_pg_collection k
                 left join langchain_pg_embedding e on e.collection_id = k.uuid
                 group by k.name order by k.name''')
    vettori = dict(c.fetchall())
print('vettori per collezione:', vettori)
for d in Document.objects.select_related('knowledge_base'):
    print(d.pk, d.status, '| chunk_count', d.chunk_count, '| chunk reali', d.chunks.count(),
          '| collezione', d.knowledge_base.collection_name)
    if d.status == 'indexed':
        assert d.chunk_count == d.chunks.count(), f'documento {d.pk} incoerente'
totale_chunk = DocumentChunk.objects.count()
print('chunk totali:', totale_chunk, '| vettori totali:', sum(vettori.values()))
assert totale_chunk == sum(vettori.values()), 'le due meta dello schema non coincidono'
print('OK')
"

# 4. Nessuna migrazione pendente, nessuna dipendenza aggiunta
.venv/Scripts/python.exe manage.py makemigrations --check --dry-run
git diff --stat requirements.in requirements.txt   # atteso: nessun output
```

**Phase Complete When:**
- [x] `rag/signals.py` esiste e i signal sono registrati da `RagConfig.ready()`
- [x] Cancellare un documento rimuove i suoi vettori da `langchain_pg_embedding` (RF-08)
- [x] Cancellare una `KnowledgeBase` rimuove i vettori di **tutti** i suoi documenti
- [x] La collezione `spike` non esiste più; `scripts/spike_rag.py` **è ancora al suo posto**
- [x] La verifica di fase manuale nel browser è stata fatta, punti (a)→(i), e il
      **report riporta cosa si è visto**, non solo che è stato fatto
- [x] `DocumentChunk.objects.count()` è uguale al numero totale di righe in
      `langchain_pg_embedding`
- [x] `requirements.in` e `requirements.txt` non sono stati toccati
- [x] Commit: `P2: rimozione dei vettori alla cancellazione e verifica di fase (T-20)`
- [x] Report completato, con la sezione «Consegna a P3»

---

## Criteri di completamento di P2

- [x] Un PDF caricato dall'admin passa a *indicizzato* mostrando pagine e
      segmenti, e i suoi chunk sono ispezionabili con pagina e ordinale (**CA-2**,
      verifica di fase del backlog)
- [x] Un PDF corrotto, vuoto o di sole immagini porta il documento a *fallito* con
      un motivo leggibile, senza compromettere l'admin (**CA-8**, RF-10)
- [x] Il testo è estratto conservando il numero di pagina (RF-03) e segmentato
      secondo il `ChunkingProfile` della base di conoscenza (RF-04, RF-19)
- [x] I segmenti sono indicizzati in pgvector e recuperabili per similarità (RF-05)
- [x] Lo stato di ogni documento è osservabile e, se fallito, motivato (RF-06, RNF-04)
- [x] Un documento può essere rielaborato senza ricaricarlo, da admin e da comando (RF-07, RF-29)
- [x] La cancellazione di un documento rimuove segmenti **e** vettori (RF-08)
- [x] Lo stesso file nella stessa base di conoscenza è rifiutato con segnalazione
      leggibile e senza creare righe (RF-09)
- [x] `manage.py ingest` copre le stesse operazioni dell'admin (RF-28)
- [x] Ogni parametro d'indice usato viene dalla configurazione: nessun
      `chunk_size`, nessun nome di modello, nessun `top_k` scritto nel codice dei
      servizi
- [x] **Cinque** commit distinti sul branch `main`, uno per fase
- [x] `makemigrations --check --dry-run` → «No changes detected»: P2 non ha
      cambiato lo schema
- [x] Nessuna dipendenza aggiunta
- [x] Report di esecuzione compilato in `plans/2026-07-24-2259-P2-plan-report.md`

---

## Consegna a P3 — cose da accertare o ricordare

| Cosa | Perché conta in P3 |
|---|---|
| `similarity_search_with_score()` restituisce una **distanza**, non una similarità: misurato **0,2041** su un match quasi esatto. Il punteggio *cresce* al peggiorare della pertinenza | RF-13 chiede un «punteggio di similarità» e RF-14 una soglia. `RetrievalProfile.score_threshold` è fra 0 e 1 e la strategia `similarity_score_threshold` di LangChain filtra su una *relevance* (1 - distanza normalizzata). T-22 e T-24 devono stabilire quale delle due grandezze mostrare e quale confrontare, e dichiararlo |
| I metadata di ogni vettore contengono `page`, `ordinal`, `document_id`, `knowledge_base_id`, `documento`, `source`, `start_index` | Sono esattamente i campi che servono a comporre le fonti di RF-13 senza una seconda interrogazione |
| `DocumentChunk.vector_id` è `"<document_id>:<ordinal>"` | T-26 (`RetrievedChunk`) può risalire dal `Document` di LangChain alla riga `DocumentChunk` con una `filter(vector_id__in=…)`, senza euristiche sul testo |
| `factories.py` esiste con `connection_string`, `get_splitter`, `get_embeddings`, `get_vectorstore` | T-21 e T-22 aggiungono `get_llm` e `get_retriever` **nello stesso file**, e la cache di T-21 dovrà coprire anche `get_vectorstore` (oggi costruito a ogni chiamata, con DDL incluso) |
| Costruire un `PGVector` esegue DDL a ogni chiamata | Sulla via calda dell'interrogazione è spreco: è un argomento in più per la cache di T-21 |
| Cold start del modello di embedding: fino a **~18 s** | La prima interrogazione dopo l'avvio sembrerà bloccata. `QueryLog.retrieval_ms` separato da `generation_ms` esiste proprio per rendere leggibile questo caso |
| `scripts/spike_rag.py` è ancora presente, la collezione `spike` no | Da cancellare alla chiusura di P3, quando `manage.py ask` lo avrà sostituito |
| L'ingestione è **sincrona** e blocca il chiamante | Non è un problema di P3, ma è il limite che T-32 rimuove e che va dichiarato nel README |

## Consegna a P6 — da argomentare nella documentazione

Si aggiungono alle due voci già lasciate da P1 (asimmetria di `base_url`,
asimmetria `PROTECT`/`CASCADE` su `KnowledgeBase`):

1. **ARCHITECTURE §6.5 va corretto in T-40.** Dice che la riconciliazione fra
   `DocumentChunk` e pgvector «avviene in un unico punto, dentro una
   transazione». Il punto unico è vero; la transazione unica **non è
   realizzabile**, perché `PGVector` ha una connessione SQLAlchemy propria. Il
   testo va sostituito con la garanzia che il progetto offre davvero: *ordine
   delle scritture scelto perché il fallimento produca vettori orfani —
   invisibili e sovrascrivibili — invece di chunk che puntano al vuoto*.
2. **La strategia di segmentazione «basata su token» è dichiarata e non
   realizzata.** Da elencare fra i limiti noti con la motivazione della
   decisione 3: `tiktoken` misurerebbe con il tokenizer di un altro modello.
3. **Il provider di embedding «huggingface» è nell'enum ma non attivabile**, per
   non introdurre torch (ARCHITECTURE §7.3). Stessa forma di limite.
4. **`EmbeddingProfile.normalize` non è applicato dal codice**, ed è corretto
   così: il modello restituisce già vettori normalizzati (misurato) e la distanza
   cosine è invariante alla scala. Detto nell'admin, va detto anche nel README —
   altrimenti sembra un campo dimenticato.
5. **RNF-03 non è ancora soddisfatto:** l'ingestione occupa il ciclo
   richiesta/risposta. È T-32; se venisse tagliato, questo diventa il limite più
   importante da dichiarare.

## Rischi noti di questa fase

| Rischio | Mitigazione |
|---|---|
| **Il primo embedding dopo l'avvio richiede fino a ~18 s** (caricamento di `bge-m3` in VRAM, misurato). L'esecutore può crederlo un blocco e interrompere | Attendere. `verify_embedding_dimension()` è la prima chiamata e paga lei il cold start, con un messaggio chiaro se il servizio non risponde |
| Costruire un `PGVector` esegue DDL (`create_tables_if_not_exists`, `create_collection`) su una connessione **non** Django | Documentato nella docstring di `get_vectorstore`. Non costruirlo dentro cicli; `create_extension=False` |
| Due connessioni al database significano che `transaction.atomic()` **non** copre pgvector | È la decisione 2: l'ordine delle scritture è scelto perché il fallimento sia recuperabile. Non «sistemarlo» spostando le scritture dentro `atomic()`: peggiorerebbe |
| `compute_checksum()` chiamata nel form lascia il puntatore del file a fine stream → si salverebbe un PDF vuoto | Il `seek(0)` finale nella funzione. **Verificare al punto 6 della fase 4** che il file salvato abbia `size > 0` |
| Su Windows, cancellare o sostituire un file mentre un'**altra** istanza di `FieldFile` sullo stesso percorso è aperta → `PermissionError [WinError 32]` (osservato in pianificazione) | `compute_checksum()` richiude ciò che ha aperto (passo 3.2). Nelle prove, `d.file.close()` prima di `d.file.delete()`. `FieldFile.delete()` chiude invece il **proprio** handle da sé |
| **`pymupdf.FileNotFoundError` ombreggia la builtin e non la sottoclassa** (deriva da `RuntimeError`): `except FileNotFoundError` non la cattura e l'errore sfugge come guasto inatteso | Passo 1.3: si cattura `pymupdf.FileNotFoundError` per nome, **prima** di `FileDataError` |
| Un POST all'admin di `Document` senza i campi `chunks-*` del management form risponde 200 senza creare nulla: una verifica di deduplica lo leggerebbe come successo | I punti 5 e 6 della fase 4 includono i quattro campi e **asseriscono** che `ManagementForm` non compaia nella risposta |
| Un `post_save` su `Document` sembra la via naturale per innescare l'ingestione, ma il servizio salva più volte → ricorsione infinita | Decisione 8: l'innesco è in `save_model()`. Se compare una ricorsione, la causa è qui |
| In `post_delete` le righe `DocumentChunk` sono già state cancellate dal collector | Decisione 9: i `vector_id` si raccolgono in `pre_delete` |
| Cancellare i vettori prima del commit di Django lascerebbe un documento vivo senza vettori se la transazione fa rollback | `transaction.on_commit()` |
| `DELETE` + `bulk_create` dei chunk azzererebbe `RetrievedChunk.chunk` (`SET_NULL`) e quindi lo storico | Decisione 10: `update_or_create` per `(document, ordinal)` |
| `separators=[]` passato esplicitamente disattiva **ogni** separatore e produce un taglio cieco | Passo 2.2: si passa `separators` solo se non vuoto |
| Con `ChunkingProfile` a `chunk_size` grande e PDF piccolo il numero di chunk resta 1: le verifiche sulla rimozione degli obsoleti sembrano passare per caso | La verifica 5 della fase 3 alterna 200/20 e 4000/200 proprio per attraversare entrambe le direzioni |
| Il PDF di prova ha **3 pagine e 3 chunk**: non esercita il batching (`batch_size=16`) | Non bloccante per P2. Un PDF più corposo per la dimostrazione è T-41, in P6. Se si vuole verificare il batching, abbassare temporaneamente `batch_size` a 1 e osservare le richieste nel log |
| `document.file.path` presuppone uno storage locale | È quello configurato. Con storage remoto servirebbe un file temporaneo: fuori ambito, da non anticipare |
| Le verifiche lasciano documenti, KB e utenti di prova nel database | Il passo 5.4 li rimuove, e la cancellazione della KB di prova è a sua volta la verifica di RF-08 in cascata |

---

## Cosa NON fare in questa fase

- **Non scrivere `get_llm()`, `get_retriever()` né alcuna catena LCEL.** Sono T-21,
  T-22, T-23, in P3. Se durante la fase 2 viene la tentazione di «già che sono in
  `factories.py`, aggiungo l'LLM», è fuori ambito: manca la cache invalidata da
  `post_save`, che è la metà interessante di T-21.
- **Non introdurre alcuna cache** nelle factory. Senza invalidazione sarebbe un
  bug in attesa; con invalidazione è T-21.
- **Non rendere l'ingestione asincrona** e non installare `django-tasks`: è T-32,
  in P5. La sincronia di P2 è dichiarata, non dimenticata.
- **Non aggiungere campi ai modelli né migrazioni.** Lo schema di P1 è completo
  per l'ingestione. Se `makemigrations` propone qualcosa, capire perché — di
  norma è un `help_text` toccato per sbaglio.
- **Non installare `tiktoken`** per far funzionare la strategia a token, e non
  installare nulla del resto: cfr. decisione 3.
- **Non cancellare `scripts/spike_rag.py`.** Solo la collezione `spike` (decisione 11).
- **Non scrivere test `pytest`** e non installarlo: è P6.
- **Non toccare `config/settings/`.** Nessun parametro di comportamento del RAG
  deve finire lì: `chunk_size`, nomi di modello e `batch_size` vivono nei profili.
- **Non usare `git add -A`**: `ARCHITECTURE.md`, `BACKLOG.md`, `PLAN.md`,
  `REQUIREMENTS.md`, `README.md` e `plans/` li aggiorna e committa l'utente a mano.
- **Non creare migrazioni che nominino `langchain_pg_collection` o
  `langchain_pg_embedding`**: quelle tabelle appartengono a `langchain-postgres`
  (ARCHITECTURE §6.3).
